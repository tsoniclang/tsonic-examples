using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_frontmatter_yaml
    {
        public static Func<string, int> indentationOf
        {
            get;
            private set;
        } = default(Func<string, int>)!;
        public static Func<string, string> yamlText
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>> splitYamlPair
        {
            get;
            private set;
        } = default(Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Action<FrontMatterMenu, string, string, string?, int> applyMenuProperty
        {
            get;
            private set;
        } = default(Action<FrontMatterMenu, string, string, string?, int>)!;
        public static Action<string, string?, int> validateYamlLine
        {
            get;
            private set;
        } = default(Action<string, string?, int>)!;
        public static Func<Tsonic.CSharp.Js.JSArray<string>, string?, FrontMatter> parseYamlFrontMatter
        {
            get;
            private set;
        } = default(Func<Tsonic.CSharp.Js.JSArray<string>, string?, FrontMatter>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_structuredScalars.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_frontmatter_data.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_frontmatter_scalars.__tsonic_module_init();
            indentationOf = (string line) =>
            {
                int indentation = 0;
                while (indentation < line.Length && line.Substring(indentation, 1) == " ")
                {
                    indentation++;
                }
                return indentation;
            };
            yamlText = (string line) => Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_structuredScalars.stripStructuredComment(line, "yaml"));
            splitYamlPair = (string text, string? sourcePath, int line) =>
            {
                int separator = Tsonic.CSharp.Js.String.indexOf(text, ":");
                if (separator <= 0)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "YAML front matter entries require 'key: value' syntax", sourcePath, line, 1);
                }
                return new Tsonic.CSharp.Js.JSArray<string>(new string[] { Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringCount(text, 0, separator)), Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringFrom(text, separator + 1)) });
            };
            applyMenuProperty = (FrontMatterMenu entry, string keyRaw, string valueRaw, string? sourcePath, int line) =>
            {
                string key = Tsonic.CSharp.Js.String.toLowerCase(keyRaw);
                if (key == "weight")
                {
                    entry.weight = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterInt(valueRaw, keyRaw, "yaml", sourcePath, line);
                }
                else
                {
                    if (key == "name")
                    {
                        entry.name = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                    }
                    else
                    {
                        if (key == "parent")
                        {
                            entry.parent = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                        }
                        else
                        {
                            if (key == "identifier")
                            {
                                entry.identifier = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                            }
                            else
                            {
                                if (key == "pre")
                                {
                                    entry.pre = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                                }
                                else
                                {
                                    if (key == "post")
                                    {
                                        entry.post = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                                    }
                                    else
                                    {
                                        if (key == "title")
                                        {
                                            entry.title = Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(valueRaw, keyRaw, "yaml", sourcePath, line);
                                        }
                                        else
                                        {
                                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_MENU_FIELD_UNKNOWN", $"Unknown front matter menu field '{keyRaw}'", sourcePath, line, 1);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            validateYamlLine = (string line, string? sourcePath, int lineNumber) =>
            {
                if (Tsonic.CSharp.Js.String.includes(line, "\t"))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "YAML front matter indentation must use spaces", sourcePath, lineNumber, 1);
                }
            };
            parseYamlFrontMatter = (Tsonic.CSharp.Js.JSArray<string> lines, string? sourcePath) =>
            {
                FrontMatter frontMatter = new FrontMatter();
                Tsonic.CSharp.Js.Set<string> rootFields = new Tsonic.CSharp.Js.Set<string>();
                int index = 0;
                while (index < lines.length)
                {
                    string raw = lines[index];
                    int lineNumber = index + 2;
                    validateYamlLine(raw, sourcePath, lineNumber);
                    string text = yamlText(raw);
                    if (text == "" || Tsonic.CSharp.Js.String.startsWith(text, "#"))
                    {
                        index++;
                        continue;
                    }
                    if (indentationOf(raw) != 0)
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "YAML front matter has an unexpected indented entry", sourcePath, lineNumber, 1);
                    }
                    Tsonic.CSharp.Js.JSArray<string> pair = splitYamlPair(text, sourcePath, lineNumber);
                    string key = pair[0];
                    string value = pair[1];
                    Node_modules_Tsumo_engine_src_frontmatter_scalars.recordFrontMatterField(rootFields, key, "Front matter", sourcePath, lineNumber);
                    if (value != "")
                    {
                        Node_modules_Tsumo_engine_src_frontmatter_scalars.applyFrontMatterScalar(frontMatter, key, value, "yaml", sourcePath, lineNumber);
                        index++;
                        continue;
                    }
                    string normalizedKey = Tsonic.CSharp.Js.String.toLowerCase(key);
                    index++;
                    if (normalizedKey == "params")
                    {
                        Tsonic.CSharp.Js.Set<string> paramFields = new Tsonic.CSharp.Js.Set<string>();
                        while (index < lines.length && indentationOf(lines[index]) > 0)
                        {
                            string childRaw = lines[index];
                            int childLine = index + 2;
                            validateYamlLine(childRaw, sourcePath, childLine);
                            string childText = yamlText(childRaw);
                            if (childText != "" && !Tsonic.CSharp.Js.String.startsWith(childText, "#"))
                            {
                                if (indentationOf(childRaw) != 2)
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "Front matter params require one scalar mapping level", sourcePath, childLine, 1);
                                }
                                Tsonic.CSharp.Js.JSArray<string> child = splitYamlPair(childText, sourcePath, childLine);
                                if (child[1] == "")
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_PARAM_INVALID", $"Front matter param '{child[0]}' requires a scalar value", sourcePath, childLine, 1);
                                }
                                Node_modules_Tsumo_engine_src_frontmatter_scalars.recordFrontMatterField(paramFields, child[0], "Front matter params", sourcePath, childLine);
                                frontMatter.Params.set(child[0], Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterParam(child[1], "yaml", sourcePath, childLine));
                            }
                            index++;
                        }
                        continue;
                    }
                    if (normalizedKey == "tags" || normalizedKey == "categories")
                    {
                        Tsonic.CSharp.Js.JSArray<string> values = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                        while (index < lines.length && indentationOf(lines[index]) > 0)
                        {
                            string childRaw_1 = lines[index];
                            int childLine_1 = index + 2;
                            validateYamlLine(childRaw_1, sourcePath, childLine_1);
                            string childText_1 = yamlText(childRaw_1);
                            if (childText_1 != "" && !Tsonic.CSharp.Js.String.startsWith(childText_1, "#"))
                            {
                                if (indentationOf(childRaw_1) != 2 || !Tsonic.CSharp.Js.String.startsWith(childText_1, "-"))
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_INVALID_STRING_ARRAY", $"Front matter field '{key}' requires a scalar list", sourcePath, childLine_1, 1);
                                }
                                string item = Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringFrom(childText_1, 1));
                                if (item == "")
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_INVALID_STRING_ARRAY", $"Front matter field '{key}' contains an empty list item", sourcePath, childLine_1, 1);
                                }
                                values.push(Node_modules_Tsumo_engine_src_frontmatter_scalars.parseFrontMatterString(item, key, "yaml", sourcePath, childLine_1));
                            }
                            index++;
                        }
                        if (normalizedKey == "tags")
                        {
                            frontMatter.tags = values;
                        }
                        else
                        {
                            frontMatter.categories = values;
                        }
                        continue;
                    }
                    if (normalizedKey == "menu")
                    {
                        Tsonic.CSharp.Js.Set<string> menuNames = new Tsonic.CSharp.Js.Set<string>();
                        while (index < lines.length && indentationOf(lines[index]) > 0)
                        {
                            string entryRaw = lines[index];
                            int entryLine = index + 2;
                            validateYamlLine(entryRaw, sourcePath, entryLine);
                            string entryText = yamlText(entryRaw);
                            if (entryText == "" || Tsonic.CSharp.Js.String.startsWith(entryText, "#"))
                            {
                                index++;
                                continue;
                            }
                            if (indentationOf(entryRaw) != 2)
                            {
                                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "Front matter menu names require one mapping level", sourcePath, entryLine, 1);
                            }
                            Tsonic.CSharp.Js.JSArray<string> entryPair = splitYamlPair(entryText, sourcePath, entryLine);
                            if (entryPair[1] != "")
                            {
                                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_MENU_INVALID", $"Front matter menu '{entryPair[0]}' requires a property mapping", sourcePath, entryLine, 1);
                            }
                            Node_modules_Tsumo_engine_src_frontmatter_scalars.recordFrontMatterField(menuNames, entryPair[0], "Front matter menu", sourcePath, entryLine);
                            FrontMatterMenu entry = new FrontMatterMenu(entryPair[0]);
                            Tsonic.CSharp.Js.Set<string> menuFields = new Tsonic.CSharp.Js.Set<string>();
                            index++;
                            while (index < lines.length && indentationOf(lines[index]) > 2)
                            {
                                string propertyRaw = lines[index];
                                int propertyLine = index + 2;
                                validateYamlLine(propertyRaw, sourcePath, propertyLine);
                                string propertyText = yamlText(propertyRaw);
                                if (propertyText != "" && !Tsonic.CSharp.Js.String.startsWith(propertyText, "#"))
                                {
                                    if (indentationOf(propertyRaw) != 4)
                                    {
                                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_YAML_SYNTAX_INVALID", "Front matter menu properties require exactly two mapping levels", sourcePath, propertyLine, 1);
                                    }
                                    Tsonic.CSharp.Js.JSArray<string> property = splitYamlPair(propertyText, sourcePath, propertyLine);
                                    if (property[1] == "")
                                    {
                                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_MENU_INVALID", $"Front matter menu field '{property[0]}' requires a scalar value", sourcePath, propertyLine, 1);
                                    }
                                    Node_modules_Tsumo_engine_src_frontmatter_scalars.recordFrontMatterField(menuFields, property[0], $"Front matter menu '{entry.menu}'", sourcePath, propertyLine);
                                    applyMenuProperty(entry, property[0], property[1], sourcePath, propertyLine);
                                }
                                index++;
                            }
                            frontMatter.menus.push(entry);
                        }
                        continue;
                    }
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_NESTED_VALUE_UNSUPPORTED", $"Front matter field '{key}' does not support a nested value", sourcePath, lineNumber, 1);
                }
                return frontMatter;
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
