using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_outputPublication
    {
        public static Func<string, string, bool, OutputPublication> beginOutputPublication
        {
            get;
            private set;
        } = default(Func<string, string, bool, OutputPublication>)!;
        public static Action<string, string, string, string> recoverOutputPublication
        {
            get;
            private set;
        } = default(Action<string, string, string, string>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_fs.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_paths.__tsonic_module_init();
            beginOutputPublication = (string siteDir, string requestedDestinationDir, bool preserveExistingOutput) =>
            {
                string siteRoot = Tsonic.CSharp.Node.path.resolve(siteDir);
                string destinationDir = Tsonic.CSharp.Node.path.isAbsolute(requestedDestinationDir) ? Tsonic.CSharp.Node.path.resolve(requestedDestinationDir) : Tsonic.CSharp.Node.path.resolve(siteRoot, requestedDestinationDir);
                if (!Tsonic.CSharp.Node.path.isAbsolute(requestedDestinationDir) && !Node_modules_Tsumo_engine_src_utils_paths.pathContainsOrEquals(siteRoot, destinationDir))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_OUTPUT_DESTINATION_ESCAPES_SITE", $"Relative output directory escapes the site root: {requestedDestinationDir}");
                }
                if (Node_modules_Tsumo_engine_src_utils_paths.pathContainsOrEquals(destinationDir, siteRoot))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_OUTPUT_DESTINATION_CONTAINS_SITE", $"Output directory cannot contain the source site: {destinationDir}");
                }
                string parent = Tsonic.CSharp.Node.path.dirname(destinationDir);
                if (parent == destinationDir)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_OUTPUT_DESTINATION_IS_ROOT", $"Output directory cannot be a filesystem root: {destinationDir}");
                }
                if (Node_modules_Tsumo_engine_src_fs.fileExists(destinationDir))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_OUTPUT_DESTINATION_IS_FILE", $"Output directory path names an existing file: {destinationDir}");
                }
                Node_modules_Tsumo_engine_src_fs.ensureDir(parent);
                string key = Tsonic.CSharp.Js.String.slice(Tsonic.CSharp.Node.crypto.createHash("sha256").update(destinationDir).digest("hex"), 0, 24);
                string scratchPrefix = $".tsumo-output-{key}";
                string backupDir = Tsonic.CSharp.Node.path.resolve(parent, $"{scratchPrefix}.backup");
                string stagePrefix = Tsonic.CSharp.Node.path.resolve(parent, $"{scratchPrefix}.stage-");
                recoverOutputPublication(destinationDir, backupDir, parent, $"{scratchPrefix}.stage-");
                string stagingDir = Tsonic.CSharp.Node.fs.mkdtempSync(stagePrefix);
                if (preserveExistingOutput && Node_modules_Tsumo_engine_src_fs.dirExists(destinationDir))
                {
                    Node_modules_Tsumo_engine_src_fs.copyDirRecursive(destinationDir, stagingDir);
                }
                return new OutputPublication(destinationDir, stagingDir, backupDir);
            };
            recoverOutputPublication = (string destinationDir, string backupDir, string parentDir, string stageNamePrefix) =>
            {
                if (Node_modules_Tsumo_engine_src_fs.fileExists(backupDir))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_OUTPUT_BACKUP_IS_FILE", $"Output publication backup path names an existing file: {backupDir}");
                }
                if (Node_modules_Tsumo_engine_src_fs.dirExists(backupDir))
                {
                    if (Node_modules_Tsumo_engine_src_fs.dirExists(destinationDir))
                    {
                        Tsonic.CSharp.Node.fs.rmSync(backupDir, new Tsonic.CSharp.Node.RmOptions
                        {
                            recursive = true,
                            force = true,
                        });
                    }
                    else
                    {
                        Tsonic.CSharp.Node.fs.renameSync(backupDir, destinationDir);
                    }
                }
                string[] entries = Tsonic.CSharp.Node.fs.readdirSync(parentDir);
                for (int index = 0; index < entries.Length; index++)
                {
                    string entry = entries[index];
                    if (Tsonic.CSharp.Js.String.startsWith(entry, stageNamePrefix))
                    {
                        Tsonic.CSharp.Node.fs.rmSync(Tsonic.CSharp.Node.path.resolve(parentDir, entry), new Tsonic.CSharp.Node.RmOptions
                        {
                            recursive = true,
                            force = true,
                        });
                    }
                }
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class OutputPublication
    {
        public string destinationDir;
        public string stagingDir;
        public string backupDir;
        public OutputPublication(string destinationDir, string stagingDir, string backupDir)
        {
            this.destinationDir = destinationDir;
            this.stagingDir = stagingDir;
            this.backupDir = backupDir;
        }
        public void publish()
        {
            bool previousOutputMoved = false;
            if (Node_modules_Tsumo_engine_src_fs.dirExists(this.destinationDir))
            {
                Tsonic.CSharp.Node.fs.renameSync(this.destinationDir, this.backupDir);
                previousOutputMoved = true;
            }
            try
            {
                Tsonic.CSharp.Node.fs.renameSync(this.stagingDir, this.destinationDir);
            }
            catch
            {
                if (previousOutputMoved && !Tsonic.CSharp.Node.fs.existsSync(this.destinationDir))
                {
                    Tsonic.CSharp.Node.fs.renameSync(this.backupDir, this.destinationDir);
                }
                throw;
            }
            if (previousOutputMoved && Node_modules_Tsumo_engine_src_fs.dirExists(this.backupDir))
            {
                Tsonic.CSharp.Node.fs.rmSync(this.backupDir, new Tsonic.CSharp.Node.RmOptions
                {
                    recursive = true,
                    force = true,
                });
            }
        }
        public void abort()
        {
            if (Node_modules_Tsumo_engine_src_fs.dirExists(this.stagingDir))
            {
                Tsonic.CSharp.Node.fs.rmSync(this.stagingDir, new Tsonic.CSharp.Node.RmOptions
                {
                    recursive = true,
                    force = true,
                });
            }
            if (!Node_modules_Tsumo_engine_src_fs.dirExists(this.backupDir))
            {
                return;
            }
            if (Node_modules_Tsumo_engine_src_fs.dirExists(this.destinationDir))
            {
                Tsonic.CSharp.Node.fs.rmSync(this.backupDir, new Tsonic.CSharp.Node.RmOptions
                {
                    recursive = true,
                    force = true,
                });
            }
            else
            {
                Tsonic.CSharp.Node.fs.renameSync(this.backupDir, this.destinationDir);
            }
        }
    }
}
