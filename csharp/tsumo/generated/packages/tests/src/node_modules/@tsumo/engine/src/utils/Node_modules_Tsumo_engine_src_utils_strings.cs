using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_utils_strings
    {
        public static void substringError()
        {
            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_INTERNAL_STRING_RANGE_INVALID", "Substring bounds are out of range");
        }
        public static Action<string, int, int> requireSubstringBounds
        {
            get;
            private set;
        } = default(Action<string, int, int>)!;
        public static Func<string, string, string, string> replaceText
        {
            get;
            private set;
        } = default(Func<string, string, string, string>)!;
        public static Func<string, string, int> indexOfText
        {
            get;
            private set;
        } = default(Func<string, string, int>)!;
        public static Func<string, string, int> indexOfTextIgnoreCase
        {
            get;
            private set;
        } = default(Func<string, string, int>)!;
        public static Func<string, string, int, int> indexOfTextFrom
        {
            get;
            private set;
        } = default(Func<string, string, int, int>)!;
        public static Func<string, string, int> lastIndexOfText
        {
            get;
            private set;
        } = default(Func<string, string, int>)!;
        public static Func<string, string, bool> containsText
        {
            get;
            private set;
        } = default(Func<string, string, bool>)!;
        public static Func<string, string, int> compareText
        {
            get;
            private set;
        } = default(Func<string, string, int>)!;
        public static Func<string, int, string> substringFrom
        {
            get;
            private set;
        } = default(Func<string, int, string>)!;
        public static Func<string, int, int, string> substringCount
        {
            get;
            private set;
        } = default(Func<string, int, int, string>)!;
        public static Func<string, int, string> charAtText
        {
            get;
            private set;
        } = default(Func<string, int, string>)!;
        public static Func<string, int, string> codePointAtText
        {
            get;
            private set;
        } = default(Func<string, int, string>)!;
        public static Func<string, int, int> nextCodePointIndex
        {
            get;
            private set;
        } = default(Func<string, int, int>)!;
        public static Func<string, int> codePointLength
        {
            get;
            private set;
        } = default(Func<string, int>)!;
        public static Func<string, int, int> utf16IndexAtCodePoint
        {
            get;
            private set;
        } = default(Func<string, int, int>)!;
        public static Func<string, int, int, string> substringCodePoints
        {
            get;
            private set;
        } = default(Func<string, int, int, string>)!;
        public static Func<string, string, string> trimStartCodePoints
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, string, string> trimEndCodePoints
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, string, string> trimCodePoints
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<double, bool> isUnicodeSpace
        {
            get;
            private set;
        } = default(Func<double, bool>)!;
        public static Func<string, string> trimUnicodeSpace
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static string zeroPadInteger(int value, int width)
        {
            string result = $"{value}";
            while (result.Length < width)
            {
                result = $"0{result}";
            }
            return result;
        }
        public static Func<string, string, string> trimStartChar
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, string, string> trimEndChar
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, string, string> replaceLineEndings
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, Tsonic.CSharp.Js.JSArray<string>> splitLines
        {
            get;
            private set;
        } = default(Func<string, Tsonic.CSharp.Js.JSArray<string>>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            requireSubstringBounds = (string source, int startIndex, int length) =>
            {
                if (startIndex < 0 || length < 0 || startIndex > source.Length || startIndex + length > source.Length)
                {
                    substringError();
                }
            };
            replaceText = (string source, string oldValue, string newValue) =>
            {
                return Tsonic.CSharp.Js.String.replaceAll(source, oldValue, newValue);
            };
            indexOfText = (string source, string value) => Tsonic.CSharp.Js.String.indexOf(source, value);
            indexOfTextIgnoreCase = (string source, string value) =>
            {
                return Tsonic.CSharp.Js.String.indexOf(Tsonic.CSharp.Js.String.toLowerCase(source), Tsonic.CSharp.Js.String.toLowerCase(value));
            };
            indexOfTextFrom = (string source, string value, int startIndex) =>
            {
                return Tsonic.CSharp.Js.String.indexOf(source, value, startIndex);
            };
            lastIndexOfText = (string source, string value) => Tsonic.CSharp.Js.String.lastIndexOf(source, value);
            containsText = (string source, string value) => Tsonic.CSharp.Js.String.includes(source, value);
            compareText = (string left, string right) =>
            {
                return string.CompareOrdinal(left, right) < 0 ? -1 : string.CompareOrdinal(left, right) > 0 ? 1 : 0;
            };
            substringFrom = (string source, int startIndex) =>
            {
                if (startIndex < 0 || startIndex > source.Length)
                {
                    substringError();
                }
                return Tsonic.CSharp.Js.String.substring(source, startIndex);
            };
            substringCount = (string source, int startIndex, int length) =>
            {
                requireSubstringBounds(source, startIndex, length);
                return Tsonic.CSharp.Js.String.substring(source, startIndex, startIndex + length);
            };
            charAtText = (string source, int index) =>
            {
                if (index < 0 || index >= source.Length)
                {
                    return "";
                }
                return Tsonic.CSharp.Js.String.substring(source, index, index + 1);
            };
            codePointAtText = (string source, int index) =>
            {
                int? codePoint = Tsonic.CSharp.Js.String.codePointAt(source, index);
                return codePoint is null ? "" : Tsonic.CSharp.Js.String.fromCodePoint(codePoint.Value);
            };
            nextCodePointIndex = (string source, int index) =>
            {
                int? codePoint = Tsonic.CSharp.Js.String.codePointAt(source, index);
                if (codePoint is null)
                {
                    return source.Length;
                }
                return (int)(index + (codePoint.Value > 65535 ? 2 : 1));
            };
            codePointLength = (string source) =>
            {
                int count = 0;
                int index = 0;
                while (index < source.Length)
                {
                    index = nextCodePointIndex(source, index);
                    count++;
                }
                return count;
            };
            utf16IndexAtCodePoint = (string source, int codePointIndex) =>
            {
                if (codePointIndex < 0)
                {
                    substringError();
                }
                int currentCodePoint = 0;
                int utf16Index = 0;
                while (currentCodePoint < codePointIndex && utf16Index < source.Length)
                {
                    utf16Index = nextCodePointIndex(source, utf16Index);
                    currentCodePoint++;
                }
                if (currentCodePoint != codePointIndex)
                {
                    substringError();
                }
                return utf16Index;
            };
            substringCodePoints = (string source, int startIndex, int length) =>
            {
                if (startIndex < 0 || length < 0)
                {
                    substringError();
                }
                int start = utf16IndexAtCodePoint(source, startIndex);
                int end = utf16IndexAtCodePoint(source, startIndex + length);
                return substringCount(source, start, end - start);
            };
            trimStartCodePoints = (string source, string cutset) =>
            {
                int start = 0;
                while (start < source.Length)
                {
                    int next = nextCodePointIndex(source, start);
                    if (!Tsonic.CSharp.Js.String.includes(cutset, substringCount(source, start, next - start)))
                    {
                        break;
                    }
                    start = next;
                }
                return substringFrom(source, start);
            };
            trimEndCodePoints = (string source, string cutset) =>
            {
                int index = 0;
                int end = 0;
                while (index < source.Length)
                {
                    int next = nextCodePointIndex(source, index);
                    if (!Tsonic.CSharp.Js.String.includes(cutset, substringCount(source, index, next - index)))
                    {
                        end = next;
                    }
                    index = next;
                }
                return substringCount(source, 0, end);
            };
            trimCodePoints = (string source, string cutset) => trimEndCodePoints(trimStartCodePoints(source, cutset), cutset);
            isUnicodeSpace = (double value) => (value >= 9 && value <= 13) || value == 32 || value == 133 || value == 160 || value == 5760 || (value >= 8192 && value <= 8202) || value == 8232 || value == 8233 || value == 8239 || value == 8287 || value == 12288;
            trimUnicodeSpace = (string source) =>
            {
                int start = 0;
                while (start < source.Length)
                {
                    int? codePoint = Tsonic.CSharp.Js.String.codePointAt(source, start);
                    if (codePoint is null || !isUnicodeSpace(codePoint.Value))
                    {
                        break;
                    }
                    start = nextCodePointIndex(source, start);
                }
                int index = start;
                int end = start;
                while (index < source.Length)
                {
                    int? codePoint_1 = Tsonic.CSharp.Js.String.codePointAt(source, index);
                    int next = nextCodePointIndex(source, index);
                    if (codePoint_1 is not null && !isUnicodeSpace(codePoint_1.Value))
                    {
                        end = next;
                    }
                    index = next;
                }
                return substringCount(source, start, end - start);
            };
            trimStartChar = (string source, string ch) =>
            {
                int start = 0;
                while (start < source.Length && Tsonic.CSharp.Js.String.substring(source, start, start + 1) == ch)
                {
                    start++;
                }
                return Tsonic.CSharp.Js.String.substring(source, start);
            };
            trimEndChar = (string source, string ch) =>
            {
                int end = source.Length;
                while (end > 0 && Tsonic.CSharp.Js.String.substring(source, end - 1, end) == ch)
                {
                    end--;
                }
                return Tsonic.CSharp.Js.String.substring(source, 0, end);
            };
            replaceLineEndings = (string source, string replacement) =>
            {
                string normalized = Tsonic.CSharp.Js.String.replaceAll(Tsonic.CSharp.Js.String.replaceAll(source, "\r\n", "\n"), "\r", "\n");
                return replacement == "\n" ? normalized : Tsonic.CSharp.Js.String.replaceAll(normalized, "\n", replacement);
            };
            splitLines = (string source) => Tsonic.CSharp.Js.String.split(replaceLineEndings(source, "\n"), "\n");
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
