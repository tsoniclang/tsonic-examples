using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_markdown_shortcodes
    {
        public static Func<ShortcodeCall, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string> renderShortcode
        {
            get;
            private set;
        } = default(Func<ShortcodeCall, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string>)!;
        public static Func<string, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string> processShortcodes
        {
            get;
            private set;
        } = default(Func<string, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string>)!;
        public static Func<string, Tsonic.CSharp.Js.JSArray<ShortcodeCall>, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string> processShortcodeCalls
        {
            get;
            private set;
        } = default(Func<string, Tsonic.CSharp.Js.JSArray<ShortcodeCall>, PageContext, SiteContext, TemplateEnvironment, ShortcodeOrdinalTracker, ShortcodeContext?, Tsonic.CSharp.Js.Map<string, bool>, string>)!;
        public static Func<ShortcodeOrdinalTracker> createOrdinalTracker
        {
            get;
            private set;
        } = default(Func<ShortcodeOrdinalTracker>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_shortcode.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_contexts.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_models.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            renderShortcode = (ShortcodeCall call, PageContext page, SiteContext site, TemplateEnvironment env, ShortcodeOrdinalTracker ordinalTracker, ShortcodeContext? parent, Tsonic.CSharp.Js.Map<string, bool> recursionGuard) =>
            {
                Template? template = env.getShortcodeTemplate(call.name);
                if (template is null)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_SHORTCODE_TEMPLATE_MISSING", $"Shortcode template not found: {call.name}", call.sourcePath ?? page.File?.Filename, call.line, call.column);
                }
                string guardKey = call.name;
                bool? isRecursing = Tsonic.CSharp.Js.Map.getValue<string, bool>(recursionGuard, guardKey);
                if (isRecursing is not null && isRecursing.Value)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_SHORTCODE_RECURSION", $"Shortcode recursion detected: {call.name}", call.sourcePath ?? page.File?.Filename, call.line, call.column);
                }
                recursionGuard.set(guardKey, true);
                int ordinal = ordinalTracker.next(call.name);
                string processedInner = call.inner;
                if (call.inner != "")
                {
                    processedInner = processShortcodes(call.inner, page, site, env, ordinalTracker, null, recursionGuard);
                }
                ShortcodeContext ctx = new ShortcodeContext(call.name, page, site, call.@params, call.positionalParams, call.isNamedParams, processedInner, ordinal, parent);
                ShortcodeValue shortcodeValue = new ShortcodeValue(ctx);
                Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> emptyOverrides = new Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>>();
                string result = env.renderTemplate(template, shortcodeValue, site, emptyOverrides);
                recursionGuard.set(guardKey, false);
                return result;
            };
            processShortcodes = (string text, PageContext page, SiteContext site, TemplateEnvironment env, ShortcodeOrdinalTracker ordinalTracker, ShortcodeContext? parent, Tsonic.CSharp.Js.Map<string, bool> recursionGuard) =>
            {
                Tsonic.CSharp.Js.JSArray<ShortcodeCall> calls = Node_modules_Tsumo_engine_src_shortcode.parseShortcodes(text, page.File?.Filename);
                if (calls.length == 0)
                {
                    return text;
                }
                return processShortcodeCalls(text, calls, page, site, env, ordinalTracker, parent, recursionGuard);
            };
            processShortcodeCalls = (string text, Tsonic.CSharp.Js.JSArray<ShortcodeCall> calls, PageContext page, SiteContext site, TemplateEnvironment env, ShortcodeOrdinalTracker ordinalTracker, ShortcodeContext? parent, Tsonic.CSharp.Js.Map<string, bool> recursionGuard) =>
            {
                Tsonic.CSharp.Js.JSArray<string> replacements = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                for (int i = 0; i < calls.length; i++)
                {
                    replacements.push(renderShortcode(calls[i], page, site, env, ordinalTracker, parent, recursionGuard));
                }
                string result = text;
                for (int i_1 = calls.length - 1; i_1 >= 0; i_1--)
                {
                    ShortcodeCall call = calls[i_1];
                    result = Node_modules_Tsumo_engine_src_utils_strings.substringCount(result, 0, call.startIndex) + replacements[i_1] + Node_modules_Tsumo_engine_src_utils_strings.substringFrom(result, call.endIndex);
                }
                return result;
            };
            createOrdinalTracker = () => new ShortcodeOrdinalTracker();
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class ShortcodeOrdinalTracker
    {
        public Tsonic.CSharp.Js.Map<string, int> counts;
        public ShortcodeOrdinalTracker()
        {
            this.counts = new Tsonic.CSharp.Js.Map<string, int>();
        }
        public int next(string name)
        {
            int? count = Tsonic.CSharp.Js.Map.getValue<string, int>(this.counts, name);
            int nextVal = (int)(count is not null ? count.Value + 1 : 0);
            this.counts.set(name, nextVal);
            return nextVal;
        }
    }
}
