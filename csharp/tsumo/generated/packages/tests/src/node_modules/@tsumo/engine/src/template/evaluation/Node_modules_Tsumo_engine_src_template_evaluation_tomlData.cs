using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_template_evaluation_tomlData
    {
        public static Func<string, string?, int, TsumoError> tomlError
        {
            get;
            private set;
        } = default(Func<string, string?, int, TsumoError>)!;
        public static Func<ParamValue, TemplateValue> scalarToTemplateValue
        {
            get;
            private set;
        } = default(Func<ParamValue, TemplateValue>)!;
        public static Func<string, bool> statementIsComplete
        {
            get;
            private set;
        } = default(Func<string, bool>)!;
        public static Func<string, string?, Tsonic.CSharp.Js.JSArray<TomlStatement>> collectTomlStatements
        {
            get;
            private set;
        } = default(Func<string, string?, Tsonic.CSharp.Js.JSArray<TomlStatement>>)!;
        public static Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>> splitTomlKey
        {
            get;
            private set;
        } = default(Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Func<string, string?, int, int> assignmentSeparator
        {
            get;
            private set;
        } = default(Func<string, string?, int, int>)!;
        public static Func<TemplateValue, string, string?, int, DictValue> requireDictionary
        {
            get;
            private set;
        } = default(Func<TemplateValue, string, string?, int, DictValue>)!;
        public static Func<DictValue, Tsonic.CSharp.Js.JSArray<string>, string?, int, DictValue> ensureDictionaryPath
        {
            get;
            private set;
        } = default(Func<DictValue, Tsonic.CSharp.Js.JSArray<string>, string?, int, DictValue>)!;
        public static Action<DictValue, Tsonic.CSharp.Js.JSArray<string>, TemplateValue, string?, int> setTomlValue
        {
            get;
            private set;
        } = default(Action<DictValue, Tsonic.CSharp.Js.JSArray<string>, TemplateValue, string?, int>)!;
        public static Func<string, string?, DictValue> parseTomlTemplateData
        {
            get;
            private set;
        } = default(Func<string, string?, DictValue>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_params.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_structuredScalars.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_values.__tsonic_module_init();
            tomlError = (string message, string? sourcePath, int line) => Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_DATA_TOML_INVALID", message, sourcePath, line, 1);
            scalarToTemplateValue = (ParamValue value) =>
            {
                if (value.kind == ParamKind.Bool)
                {
                    return new BoolValue(value.boolValue);
                }
                if (value.kind == ParamKind.Number)
                {
                    return new NumberValue(value.numberValue);
                }
                return new StringValue(value.stringValue);
            };
            statementIsComplete = (string text) =>
            {
                int squareDepth = 0;
                int objectDepth = 0;
                string quote = "";
                bool escaped = false;
                for (int index = 0; index < text.Length; index++)
                {
                    string character = text.Substring(index, 1);
                    if (escaped)
                    {
                        escaped = false;
                        continue;
                    }
                    if (quote == "\"" && character == "\\")
                    {
                        escaped = true;
                        continue;
                    }
                    if (character == "\"" || character == "'")
                    {
                        if (quote == "")
                        {
                            quote = character;
                        }
                        else
                        {
                            if (quote == character)
                            {
                                quote = "";
                            }
                        }
                        continue;
                    }
                    if (quote != "")
                    {
                        continue;
                    }
                    if (character == "[")
                    {
                        squareDepth++;
                    }
                    else
                    {
                        if (character == "]")
                        {
                            squareDepth--;
                        }
                        else
                        {
                            if (character == "{")
                            {
                                objectDepth++;
                            }
                            else
                            {
                                if (character == "}")
                                {
                                    objectDepth--;
                                }
                            }
                        }
                    }
                    if (squareDepth < 0 || objectDepth < 0)
                    {
                        return true;
                    }
                }
                return quote == "" && squareDepth == 0 && objectDepth == 0;
            };
            collectTomlStatements = (string text, string? sourcePath) =>
            {
                string normalized = Tsonic.CSharp.Js.String.replaceAll(Tsonic.CSharp.Js.String.replaceAll(text, "\r\n", "\n"), "\r", "\n");
                Tsonic.CSharp.Js.JSArray<string> lines = Tsonic.CSharp.Js.String.split(normalized, "\n");
                Tsonic.CSharp.Js.JSArray<TomlStatement> statements = new Tsonic.CSharp.Js.JSArray<TomlStatement>(new TomlStatement[] { });
                string pending = "";
                int pendingLine = 0;
                for (int index = 0; index < lines.length; index++)
                {
                    string line = Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_structuredScalars.stripStructuredComment(lines[index], "toml"));
                    if (line == "")
                    {
                        continue;
                    }
                    if (pending == "")
                    {
                        pending = line;
                        pendingLine = index + 1;
                    }
                    else
                    {
                        pending += " " + line;
                    }
                    if (!statementIsComplete(pending))
                    {
                        continue;
                    }
                    statements.push(new TomlStatement(pending, pendingLine));
                    pending = "";
                    pendingLine = 0;
                }
                if (pending != "")
                {
                    throw tomlError("TOML statement is incomplete", sourcePath, pendingLine);
                }
                return statements;
            };
            splitTomlKey = (string text, string? sourcePath, int line) =>
            {
                Tsonic.CSharp.Js.JSArray<string> segments = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                int start = 0;
                string quote = "";
                bool escaped = false;
                for (int index = 0; index <= text.Length; index++)
                {
                    string character = index < text.Length ? text.Substring(index, 1) : ".";
                    if (escaped)
                    {
                        escaped = false;
                        continue;
                    }
                    if (quote == "\"" && character == "\\")
                    {
                        escaped = true;
                        continue;
                    }
                    if (character == "\"" || character == "'")
                    {
                        if (quote == "")
                        {
                            quote = character;
                        }
                        else
                        {
                            if (quote == character)
                            {
                                quote = "";
                            }
                        }
                        continue;
                    }
                    if (character != "." || quote != "")
                    {
                        continue;
                    }
                    string raw = Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(text, start, index));
                    if (raw == "")
                    {
                        throw tomlError("TOML key segment cannot be empty", sourcePath, line);
                    }
                    if (Tsonic.CSharp.Js.String.startsWith(raw, "\"") || Tsonic.CSharp.Js.String.startsWith(raw, "'"))
                    {
                        ParamValue parsed = Node_modules_Tsumo_engine_src_utils_structuredScalars.parseStructuredScalar(raw, "toml", (string message) => tomlError(message, sourcePath, line));
                        if (parsed.kind != ParamKind.String)
                        {
                            throw tomlError("Quoted TOML key must be a string", sourcePath, line);
                        }
                        segments.push(parsed.stringValue);
                    }
                    else
                    {
                        if (!new Tsonic.CSharp.Js.RegExp("^[A-Za-z0-9_-]+$", "").test(raw))
                        {
                            throw tomlError($"TOML key segment '{raw}' is invalid", sourcePath, line);
                        }
                        segments.push(raw);
                    }
                    start = index + 1;
                }
                if (quote != "")
                {
                    throw tomlError("TOML key contains an unterminated quote", sourcePath, line);
                }
                return segments;
            };
            assignmentSeparator = (string text, string? sourcePath, int line) =>
            {
                int squareDepth = 0;
                int objectDepth = 0;
                string quote = "";
                bool escaped = false;
                for (int index = 0; index < text.Length; index++)
                {
                    string character = text.Substring(index, 1);
                    if (escaped)
                    {
                        escaped = false;
                        continue;
                    }
                    if (quote == "\"" && character == "\\")
                    {
                        escaped = true;
                        continue;
                    }
                    if (character == "\"" || character == "'")
                    {
                        if (quote == "")
                        {
                            quote = character;
                        }
                        else
                        {
                            if (quote == character)
                            {
                                quote = "";
                            }
                        }
                        continue;
                    }
                    if (quote != "")
                    {
                        continue;
                    }
                    if (character == "[")
                    {
                        squareDepth++;
                    }
                    else
                    {
                        if (character == "]")
                        {
                            squareDepth--;
                        }
                        else
                        {
                            if (character == "{")
                            {
                                objectDepth++;
                            }
                            else
                            {
                                if (character == "}")
                                {
                                    objectDepth--;
                                }
                                else
                                {
                                    if (character == "=" && squareDepth == 0 && objectDepth == 0)
                                    {
                                        return index;
                                    }
                                }
                            }
                        }
                    }
                }
                throw tomlError("TOML assignment requires an '=' separator", sourcePath, line);
            };
            requireDictionary = (TemplateValue value, string context, string? sourcePath, int line) =>
            {
                if (value is DictValue)
                {
                    return (DictValue)value;
                }
                throw tomlError($"{context} conflicts with a non-table value", sourcePath, line);
            };
            ensureDictionaryPath = (DictValue root, Tsonic.CSharp.Js.JSArray<string> segments, string? sourcePath, int line) =>
            {
                DictValue current = root;
                for (int index = 0; index < segments.length; index++)
                {
                    string segment = segments[index];
                    TemplateValue? existing = Tsonic.CSharp.Js.Map.getReference<string, TemplateValue>(current.value, segment);
                    if (existing is null)
                    {
                        DictValue created = new DictValue(new Tsonic.CSharp.Js.Map<string, TemplateValue>());
                        current.value.set(segment, created);
                        current = created;
                        continue;
                    }
                    current = requireDictionary(existing, $"TOML table '{Tsonic.CSharp.Js.Array.join(segments, ".")}'", sourcePath, line);
                }
                return current;
            };
            setTomlValue = (DictValue table, Tsonic.CSharp.Js.JSArray<string> key, TemplateValue value, string? sourcePath, int line) =>
            {
                Tsonic.CSharp.Js.JSArray<string> parentSegments = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                for (int index = 0; index < key.length - 1; index++)
                {
                    parentSegments.push(key[index]);
                }
                DictValue parent = ensureDictionaryPath(table, parentSegments, sourcePath, line);
                string name = key[key.length - 1];
                if (parent.value.has(name))
                {
                    throw tomlError($"TOML key '{Tsonic.CSharp.Js.Array.join(key, ".")}' is declared more than once", sourcePath, line);
                }
                parent.value.set(name, value);
            };
            parseTomlTemplateData = (string text, string? sourcePath) =>
            {
                DictValue root = new DictValue(new Tsonic.CSharp.Js.Map<string, TemplateValue>());
                DictValue currentTable = root;
                Tsonic.CSharp.Js.Set<string> declaredTables = new Tsonic.CSharp.Js.Set<string>();
                Tsonic.CSharp.Js.JSArray<TomlStatement> statements = collectTomlStatements(text, sourcePath);
                for (int index = 0; index < statements.length; index++)
                {
                    TomlStatement statement = statements[index];
                    string raw = Tsonic.CSharp.Js.String.trim(statement.text);
                    if (Tsonic.CSharp.Js.String.startsWith(raw, "[[") && Tsonic.CSharp.Js.String.endsWith(raw, "]]"))
                    {
                        Tsonic.CSharp.Js.JSArray<string> path = splitTomlKey(Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(raw, 2, raw.Length - 2)), sourcePath, statement.line);
                        if (path.length == 0)
                        {
                            throw tomlError("TOML array table name cannot be empty", sourcePath, statement.line);
                        }
                        Tsonic.CSharp.Js.JSArray<string> parentSegments = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                        for (int pathIndex = 0; pathIndex < path.length - 1; pathIndex++)
                        {
                            parentSegments.push(path[pathIndex]);
                        }
                        DictValue parent = ensureDictionaryPath(root, parentSegments, sourcePath, statement.line);
                        string name = path[path.length - 1];
                        TemplateValue? existing = Tsonic.CSharp.Js.Map.getReference<string, TemplateValue>(parent.value, name);
                        AnyArrayValue entries = default(AnyArrayValue)!;
                        if (existing is null)
                        {
                            entries = new AnyArrayValue(new Tsonic.CSharp.Js.JSArray<TemplateValue>(new TemplateValue[] { }));
                            parent.value.set(name, entries);
                        }
                        else
                        {
                            if (existing is AnyArrayValue)
                            {
                                entries = (AnyArrayValue)existing;
                            }
                            else
                            {
                                throw tomlError($"TOML array table '{Tsonic.CSharp.Js.Array.join(path, ".")}' conflicts with another value", sourcePath, statement.line);
                            }
                        }
                        currentTable = new DictValue(new Tsonic.CSharp.Js.Map<string, TemplateValue>());
                        entries.value.push(currentTable);
                        continue;
                    }
                    if (Tsonic.CSharp.Js.String.startsWith(raw, "[") && Tsonic.CSharp.Js.String.endsWith(raw, "]"))
                    {
                        Tsonic.CSharp.Js.JSArray<string> path_1 = splitTomlKey(Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(raw, 1, raw.Length - 1)), sourcePath, statement.line);
                        string identity = Tsonic.CSharp.Js.Array.join(path_1, ".");
                        if (declaredTables.has(identity))
                        {
                            throw tomlError($"TOML table '{identity}' is declared more than once", sourcePath, statement.line);
                        }
                        declaredTables.add(identity);
                        currentTable = ensureDictionaryPath(root, path_1, sourcePath, statement.line);
                        continue;
                    }
                    int separator = assignmentSeparator(raw, sourcePath, statement.line);
                    Tsonic.CSharp.Js.JSArray<string> key = splitTomlKey(Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(raw, 0, separator)), sourcePath, statement.line);
                    string valueText = Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(raw, separator + 1));
                    TemplateValue value = new TomlValueReader(valueText, sourcePath, statement.line).parse();
                    setTomlValue(currentTable, key, value, sourcePath, statement.line);
                }
                return root;
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class TomlStatement
    {
        public string text;
        public int line;
        public TomlStatement(string text, int line)
        {
            this.text = text;
            this.line = line;
        }
    }
    public class TomlValueReader
    {
        public string text;
        public int index;
        public string? sourcePath;
        public int line;
        public TomlValueReader(string text, string? sourcePath, int line)
        {
            this.text = text;
            this.index = 0;
            this.sourcePath = sourcePath;
            this.line = line;
        }
        public TemplateValue parse()
        {
            TemplateValue value = this.parseValue();
            this.skipWhitespace();
            if (this.index != this.text.Length)
            {
                throw this.error("Unexpected trailing TOML value content");
            }
            return value;
        }
        public TemplateValue parseValue()
        {
            this.skipWhitespace();
            string character = this.peek();
            if (character == "\"" || character == "'")
            {
                return this.parseString();
            }
            if (character == "[")
            {
                return this.parseArray();
            }
            if (character == "{")
            {
                return this.parseInlineTable();
            }
            return this.parseBareScalar();
        }
        public TemplateValue parseString()
        {
            int start = this.index;
            string quote = this.next();
            if (this.peek() == quote && this.index + 1 < this.text.Length && this.text.Substring(this.index + 1, 1) == quote)
            {
                throw this.error("Multiline TOML strings are not supported by the data contract");
            }
            bool escaped = false;
            while (this.index < this.text.Length)
            {
                string character = this.next();
                if (escaped)
                {
                    escaped = false;
                    continue;
                }
                if (quote == "\"" && character == "\\")
                {
                    escaped = true;
                    continue;
                }
                if (character != quote)
                {
                    continue;
                }
                string raw = Tsonic.CSharp.Js.String.slice(this.text, start, this.index);
                string? sourcePath = this.sourcePath;
                int line = this.line;
                return Node_modules_Tsumo_engine_src_template_evaluation_tomlData.scalarToTemplateValue(Node_modules_Tsumo_engine_src_utils_structuredScalars.parseStructuredScalar(raw, "toml", (string message) => Node_modules_Tsumo_engine_src_template_evaluation_tomlData.tomlError(message, sourcePath, line)));
            }
            throw this.error("TOML string is unterminated");
        }
        public TemplateValue parseArray()
        {
            this.expect("[");
            Tsonic.CSharp.Js.JSArray<TemplateValue> items = new Tsonic.CSharp.Js.JSArray<TemplateValue>(new TemplateValue[] { });
            this.skipWhitespace();
            if (this.peek() == "]")
            {
                this.index++;
                return new AnyArrayValue(items);
            }
            while (true)
            {
                items.push(this.parseValue());
                this.skipWhitespace();
                string separator = this.peek();
                if (separator == "]")
                {
                    this.index++;
                    return new AnyArrayValue(items);
                }
                if (separator != ",")
                {
                    throw this.error("TOML array entries must be separated by commas");
                }
                this.index++;
                this.skipWhitespace();
                if (this.peek() == "]")
                {
                    this.index++;
                    return new AnyArrayValue(items);
                }
            }
        }
        public TemplateValue parseInlineTable()
        {
            this.expect("{");
            DictValue fields = new DictValue(new Tsonic.CSharp.Js.Map<string, TemplateValue>());
            this.skipWhitespace();
            if (this.peek() == "}")
            {
                this.index++;
                return fields;
            }
            while (true)
            {
                int keyStart = this.index;
                string quote = "";
                bool escaped = false;
                while (this.index < this.text.Length)
                {
                    string character = this.peek();
                    if (escaped)
                    {
                        escaped = false;
                        this.index++;
                        continue;
                    }
                    if (quote == "\"" && character == "\\")
                    {
                        escaped = true;
                        this.index++;
                        continue;
                    }
                    if (character == "\"" || character == "'")
                    {
                        if (quote == "")
                        {
                            quote = character;
                        }
                        else
                        {
                            if (quote == character)
                            {
                                quote = "";
                            }
                        }
                        this.index++;
                        continue;
                    }
                    if (character == "=" && quote == "")
                    {
                        break;
                    }
                    this.index++;
                }
                if (this.peek() != "=")
                {
                    throw this.error("TOML inline table entry requires '='");
                }
                Tsonic.CSharp.Js.JSArray<string> key = Node_modules_Tsumo_engine_src_template_evaluation_tomlData.splitTomlKey(Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(this.text, keyStart, this.index)), this.sourcePath, this.line);
                this.index++;
                Node_modules_Tsumo_engine_src_template_evaluation_tomlData.setTomlValue(fields, key, this.parseValue(), this.sourcePath, this.line);
                this.skipWhitespace();
                string separator = this.peek();
                if (separator == "}")
                {
                    this.index++;
                    return fields;
                }
                if (separator != ",")
                {
                    throw this.error("TOML inline table entries must be separated by commas");
                }
                this.index++;
                this.skipWhitespace();
                if (this.peek() == "}")
                {
                    throw this.error("TOML inline tables do not allow a trailing comma");
                }
            }
        }
        public TemplateValue parseBareScalar()
        {
            int start = this.index;
            while (this.index < this.text.Length)
            {
                string character = this.peek();
                if (character == "," || character == "]" || character == "}" || new Tsonic.CSharp.Js.RegExp("\\s", "").test(character))
                {
                    break;
                }
                this.index++;
            }
            string raw = Tsonic.CSharp.Js.String.trim(Tsonic.CSharp.Js.String.slice(this.text, start, this.index));
            if (raw == "")
            {
                throw this.error("TOML value cannot be empty");
            }
            string? sourcePath = this.sourcePath;
            int line = this.line;
            try
            {
                return Node_modules_Tsumo_engine_src_template_evaluation_tomlData.scalarToTemplateValue(Node_modules_Tsumo_engine_src_utils_structuredScalars.parseStructuredScalar(raw, "toml", (string message) => Node_modules_Tsumo_engine_src_template_evaluation_tomlData.tomlError(message, sourcePath, line)));
            }
            catch
            {
                if (new Tsonic.CSharp.Js.RegExp("^\\d{4}-\\d{2}-\\d{2}(?:[Tt ][0-9:.+-]+[Zz]?)?$", "").test(raw) || new Tsonic.CSharp.Js.RegExp("^\\d{2}:\\d{2}:\\d{2}(?:\\.\\d+)?$", "").test(raw))
                {
                    return new StringValue(raw);
                }
                throw;
            }
        }
        public void skipWhitespace()
        {
            while (this.index < this.text.Length && new Tsonic.CSharp.Js.RegExp("\\s", "").test(this.text.Substring(this.index, 1)))
            {
                this.index++;
            }
        }
        public string peek()
        {
            return this.index < this.text.Length ? this.text.Substring(this.index, 1) : "";
        }
        public string next()
        {
            if (this.index >= this.text.Length)
            {
                throw this.error("Unexpected end of TOML value");
            }
            return this.text.Substring(this.index++, 1);
        }
        public void expect(string character)
        {
            if (this.next() != character)
            {
                throw this.error($"Expected '{character}'");
            }
        }
        public TsumoError error(string message)
        {
            return Node_modules_Tsumo_engine_src_template_evaluation_tomlData.tomlError(message, this.sourcePath, this.line);
        }
    }
}
