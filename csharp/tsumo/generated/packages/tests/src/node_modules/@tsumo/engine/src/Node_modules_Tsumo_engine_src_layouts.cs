using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_layouts
    {
        public static Func<LayoutEnvironment, Tsonic.CSharp.Js.JSArray<string>, string?> selectTemplatePath
        {
            get;
            private set;
        } = default(Func<LayoutEnvironment, Tsonic.CSharp.Js.JSArray<string>, string?>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_utils_textBuilder.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_fs.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_index.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_i18n.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_models.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_scope.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_embeddedTemplates.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_template_paths.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_paths.__tsonic_module_init();
            selectTemplatePath = (LayoutEnvironment environment, Tsonic.CSharp.Js.JSArray<string> candidates) =>
            {
                for (int index = 0; index < candidates.length; index++)
                {
                    string candidate = candidates[index];
                    if (environment.getTemplate(candidate) is not null)
                    {
                        return candidate;
                    }
                }
                return null;
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class LayoutEnvironment : TemplateEnvironment
    {
        public string siteLayoutsDir;
        public string? themeLayoutsDir;
        public Tsonic.CSharp.Js.JSArray<string> mountedLayoutDirs;
        public Tsonic.CSharp.Js.Map<string, Template> cache;
        public Tsonic.CSharp.Js.Map<string, Template> shortcodeCache;
        public Tsonic.CSharp.Js.Map<string, Template> renderHookCache;
        public I18nStore i18nStore;
        public LayoutEnvironment(string siteDir, string? themeDirRaw, Tsonic.CSharp.Js.JSArray<ModuleMount>? mountsRaw = null, Tsonic.CSharp.Js.Date? buildTime = null, DictValue? siteData = null) : base(buildTime, siteData)
        {
            string? themeDir = themeDirRaw;
            Tsonic.CSharp.Js.JSArray<ModuleMount>? mounts = mountsRaw;
            this.siteLayoutsDir = Tsonic.CSharp.Node.path.join(siteDir, "layouts");
            this.themeLayoutsDir = themeDir is not null ? Tsonic.CSharp.Node.path.join(themeDir, "layouts") : null;
            this.mountedLayoutDirs = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
            this.cache = new Tsonic.CSharp.Js.Map<string, Template>();
            this.shortcodeCache = new Tsonic.CSharp.Js.Map<string, Template>();
            this.renderHookCache = new Tsonic.CSharp.Js.Map<string, Template>();
            this.i18nStore = new I18nStore();
            if (themeDir is not null)
            {
                this.i18nStore.loadFromDir(Tsonic.CSharp.Node.path.join(themeDir, "i18n"));
            }
            if (mounts is not null)
            {
                for (int i = mounts.length - 1; i >= 0; i--)
                {
                    ModuleMount mount = mounts[i];
                    if (mount.target != "i18n")
                    {
                        continue;
                    }
                    string mountPath = Tsonic.CSharp.Node.path.isAbsolute(mount.source) ? mount.source : Tsonic.CSharp.Node.path.join(siteDir, mount.source);
                    if (Node_modules_Tsumo_engine_src_fs.dirExists(mountPath))
                    {
                        this.i18nStore.loadFromDir(mountPath);
                    }
                }
            }
            this.i18nStore.loadFromDir(Tsonic.CSharp.Node.path.join(siteDir, "i18n"));
            if (mounts is not null)
            {
                for (int i_1 = 0; i_1 < mounts.length; i_1++)
                {
                    ModuleMount mount_1 = mounts[i_1];
                    if (mount_1.target == "layouts")
                    {
                        string mountPath_1 = Tsonic.CSharp.Node.path.isAbsolute(mount_1.source) ? mount_1.source : Tsonic.CSharp.Node.path.join(siteDir, mount_1.source);
                        if (Node_modules_Tsumo_engine_src_fs.dirExists(mountPath_1))
                        {
                            this.mountedLayoutDirs.push(mountPath_1);
                        }
                    }
                }
            }
        }
        public override ResourceManager? getResourceManager()
        {
            return null;
        }
        public override string renderTextTemplateSource(string source, TemplateValue context, SiteContext site, Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> overrides, RenderState? state = null)
        {
            return this.renderTextTemplate(Node_modules_Tsumo_engine_src_template_parser_parseTemplate.parseTemplate(source, null), context, site, overrides, state);
        }
        public override string renderTemplate(Template template, TemplateValue context, SiteContext site, Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> overrides, RenderState? state = null)
        {
            TextBuilder output = new TextBuilder();
            RenderScope scope = new RenderScope(context, context, site, this, null, state, template.sourcePath);
            template.renderInto(output, scope, this, overrides);
            return output.toString();
        }
        public override string renderTextTemplate(Template template, TemplateValue context, SiteContext site, Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> overrides, RenderState? state = null)
        {
            TextBuilder output = new TextBuilder();
            RenderScope scope = new RenderScope(context, context, site, this, null, state, template.sourcePath);
            template.renderTextInto(output, scope, this, overrides);
            return output.toString();
        }
        public override string renderTemplateDefinition(Tsonic.CSharp.Js.JSArray<TemplateNode> nodes, Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> definitions, string? sourcePath, TemplateValue context, SiteContext site, Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>> overrides, RenderState? state = null)
        {
            return this.renderTemplate(new Template(nodes, definitions, sourcePath), context, site, overrides, state);
        }
        public override Template? getTemplate(string relPathRaw)
        {
            string slash = "/";
            string relPath = Node_modules_Tsumo_engine_src_template_paths.normalizeTemplateRelativePath(Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.trimStartChar(relPathRaw, slash)));
            Tsonic.CSharp.Js.JSArray<string> relativePaths = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
            if (Tsonic.CSharp.Node.path.extname(relPath) != "")
            {
                relativePaths.push(relPath);
            }
            else
            {
                relativePaths.push(relPath + ".html");
                relativePaths.push(relPath + ".htm");
            }
            Tsonic.CSharp.Js.JSArray<string> candidates = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
            for (int i = 0; i < relativePaths.length; i++)
            {
                candidates.push(Tsonic.CSharp.Node.path.join(this.siteLayoutsDir, Node_modules_Tsumo_engine_src_utils_strings.replaceText(relativePaths[i], slash, $"{Tsonic.CSharp.Node.path.sep}")));
            }
            string? themeLayoutsDir = this.themeLayoutsDir;
            if (themeLayoutsDir is not null)
            {
                for (int i_1 = 0; i_1 < relativePaths.length; i_1++)
                {
                    candidates.push(Tsonic.CSharp.Node.path.join(themeLayoutsDir, Node_modules_Tsumo_engine_src_utils_strings.replaceText(relativePaths[i_1], slash, $"{Tsonic.CSharp.Node.path.sep}")));
                }
            }
            for (int i_2 = 0; i_2 < this.mountedLayoutDirs.length; i_2++)
            {
                for (int pathIndex = 0; pathIndex < relativePaths.length; pathIndex++)
                {
                    candidates.push(Tsonic.CSharp.Node.path.join(this.mountedLayoutDirs[i_2], Node_modules_Tsumo_engine_src_utils_strings.replaceText(relativePaths[pathIndex], slash, $"{Tsonic.CSharp.Node.path.sep}")));
                }
            }
            string? resolved = null;
            for (int i_3 = 0; i_3 < candidates.length; i_3++)
            {
                string candidate = candidates[i_3];
                if (Node_modules_Tsumo_engine_src_fs.fileExists(candidate))
                {
                    resolved = candidate;
                    break;
                }
            }
            if (resolved is null)
            {
                string? embeddedPath = null;
                string? embeddedSource = null;
                for (int i_4 = 0; i_4 < relativePaths.length; i_4++)
                {
                    string? candidateSource = Node_modules_Tsumo_engine_src_template_embeddedTemplates.getEmbeddedTemplateSource(relativePaths[i_4]);
                    if (candidateSource is null)
                    {
                        continue;
                    }
                    embeddedPath = relativePaths[i_4];
                    embeddedSource = candidateSource;
                    break;
                }
                if (embeddedSource is null || embeddedPath is null)
                {
                    return null;
                }
                string embeddedKey = $"embedded:{Tsonic.CSharp.Js.String.toLowerCase(embeddedPath)}";
                Template? embeddedCached = Tsonic.CSharp.Js.Map.getReference<string, Template>(this.cache, embeddedKey);
                if (embeddedCached is not null)
                {
                    return embeddedCached;
                }
                Template embedded = Node_modules_Tsumo_engine_src_template_parser_parseTemplate.parseTemplate(embeddedSource, embeddedKey);
                this.cache.set(embeddedKey, embedded);
                return embedded;
            }
            Template? cached = Tsonic.CSharp.Js.Map.getReference<string, Template>(this.cache, resolved);
            if (cached is not null)
            {
                return cached;
            }
            string text = Node_modules_Tsumo_engine_src_fs.readTextFile(resolved);
            Template tpl = Node_modules_Tsumo_engine_src_template_parser_parseTemplate.parseTemplate(text, resolved);
            this.cache.set(resolved, tpl);
            return tpl;
        }
        public override string? getTemplateSourceRelativePath(string sourcePath)
        {
            string source = Tsonic.CSharp.Node.path.resolve(sourcePath);
            Tsonic.CSharp.Js.JSArray<string> roots = new Tsonic.CSharp.Js.JSArray<string>(new string[] { this.siteLayoutsDir });
            string? themeLayoutsDir = this.themeLayoutsDir;
            if (themeLayoutsDir is not null)
            {
                roots.push(themeLayoutsDir);
            }
            for (int index = 0; index < this.mountedLayoutDirs.length; index++)
            {
                roots.push(this.mountedLayoutDirs[index]);
            }
            for (int index_1 = 0; index_1 < roots.length; index_1++)
            {
                string root = Tsonic.CSharp.Node.path.resolve(roots[index_1]);
                if (!Node_modules_Tsumo_engine_src_utils_paths.pathContainsOrEquals(root, source))
                {
                    continue;
                }
                return Node_modules_Tsumo_engine_src_template_paths.normalizeTemplateRelativePath(Node_modules_Tsumo_engine_src_utils_strings.replaceText(Tsonic.CSharp.Node.path.relative(root, source), "\\", "/"));
            }
            return null;
        }
        public override string? renderPageView(PageContext page, string viewRaw, RenderState? state)
        {
            string view = Node_modules_Tsumo_engine_src_template_paths.normalizeTemplateRelativePath(viewRaw);
            if (view == "")
            {
                return null;
            }
            Tsonic.CSharp.Js.JSArray<string> candidates = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
            if (Tsonic.CSharp.Js.String.trim(page.type) != "")
            {
                candidates.push($"{page.type}/{view}");
            }
            if (Tsonic.CSharp.Js.String.trim(page.section) != "" && page.section != page.type)
            {
                candidates.push($"{page.section}/{view}");
            }
            candidates.push($"_default/{view}");
            candidates.push(view);
            string? templatePath = Node_modules_Tsumo_engine_src_layouts.selectTemplatePath(this, candidates);
            if (templatePath is null)
            {
                return null;
            }
            Template? template = this.getTemplate(templatePath);
            if (template is null)
            {
                return null;
            }
            PageValue context = new PageValue(page);
            return this.renderTemplate(template, context, page.site, new Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<TemplateNode>>(), state);
        }
        public override Template? getShortcodeTemplate(string name)
        {
            Template? cached = Tsonic.CSharp.Js.Map.getReference<string, Template>(this.shortcodeCache, name);
            if (cached is not null)
            {
                return cached;
            }
            Tsonic.CSharp.Js.JSArray<string> candidates = new Tsonic.CSharp.Js.JSArray<string>(new string[] { Tsonic.CSharp.Node.path.join(this.siteLayoutsDir, "shortcodes", name + ".html"), Tsonic.CSharp.Node.path.join(this.siteLayoutsDir, "_shortcodes", name + ".html") });
            string? themeLayoutsDir = this.themeLayoutsDir;
            if (themeLayoutsDir is not null)
            {
                candidates.push(Tsonic.CSharp.Node.path.join(themeLayoutsDir, "shortcodes", name + ".html"));
                candidates.push(Tsonic.CSharp.Node.path.join(themeLayoutsDir, "_shortcodes", name + ".html"));
            }
            for (int i = 0; i < this.mountedLayoutDirs.length; i++)
            {
                string dir = this.mountedLayoutDirs[i];
                candidates.push(Tsonic.CSharp.Node.path.join(dir, "shortcodes", name + ".html"));
                candidates.push(Tsonic.CSharp.Node.path.join(dir, "_shortcodes", name + ".html"));
            }
            string? resolved = null;
            for (int i_1 = 0; i_1 < candidates.length; i_1++)
            {
                string candidate = candidates[i_1];
                if (Node_modules_Tsumo_engine_src_fs.fileExists(candidate))
                {
                    resolved = candidate;
                    break;
                }
            }
            if (resolved is null)
            {
                return null;
            }
            Template tpl = Node_modules_Tsumo_engine_src_template_parser_parseTemplate.parseTemplate(Node_modules_Tsumo_engine_src_fs.readTextFile(resolved), resolved);
            this.shortcodeCache.set(name, tpl);
            return tpl;
        }
        public override Template? getRenderHookTemplate(string hookName)
        {
            Template? cached = Tsonic.CSharp.Js.Map.getReference<string, Template>(this.renderHookCache, hookName);
            if (cached is not null)
            {
                return cached;
            }
            Tsonic.CSharp.Js.JSArray<string> candidates = new Tsonic.CSharp.Js.JSArray<string>(new string[] { Tsonic.CSharp.Node.path.join(this.siteLayoutsDir, "_markup", hookName + ".html"), Tsonic.CSharp.Node.path.join(this.siteLayoutsDir, "_default", "_markup", hookName + ".html") });
            string? themeLayoutsDir = this.themeLayoutsDir;
            if (themeLayoutsDir is not null)
            {
                candidates.push(Tsonic.CSharp.Node.path.join(themeLayoutsDir, "_markup", hookName + ".html"));
                candidates.push(Tsonic.CSharp.Node.path.join(themeLayoutsDir, "_default", "_markup", hookName + ".html"));
            }
            for (int i = 0; i < this.mountedLayoutDirs.length; i++)
            {
                string dir = this.mountedLayoutDirs[i];
                candidates.push(Tsonic.CSharp.Node.path.join(dir, "_markup", hookName + ".html"));
                candidates.push(Tsonic.CSharp.Node.path.join(dir, "_default", "_markup", hookName + ".html"));
            }
            string? resolved = null;
            for (int i_1 = 0; i_1 < candidates.length; i_1++)
            {
                string candidate = candidates[i_1];
                if (Node_modules_Tsumo_engine_src_fs.fileExists(candidate))
                {
                    resolved = candidate;
                    break;
                }
            }
            if (resolved is null)
            {
                return null;
            }
            Template tpl = Node_modules_Tsumo_engine_src_template_parser_parseTemplate.parseTemplate(Node_modules_Tsumo_engine_src_fs.readTextFile(resolved), resolved);
            this.renderHookCache.set(hookName, tpl);
            return tpl;
        }
        public override string getI18n(string lang, string key, int? count = null)
        {
            return this.i18nStore.translate(lang, key, count);
        }
    }
}
