using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_markdown_toc
    {
        public static Func<int, string> indent
        {
            get;
            private set;
        } = default(Func<int, string>)!;
        public static Action<Markdig.Syntax.Inlines.Inline, System.Text.StringBuilder> appendInlinePlainText
        {
            get;
            private set;
        } = default(Action<Markdig.Syntax.Inlines.Inline, System.Text.StringBuilder>)!;
        public static Func<Markdig.Syntax.HeadingBlock, string> getHeadingPlainText
        {
            get;
            private set;
        } = default(Func<Markdig.Syntax.HeadingBlock, string>)!;
        public static Func<Markdig.Syntax.ContainerBlock, Tsonic.CSharp.Js.JSArray<TocHeading>> collectHeadingsFromAst
        {
            get;
            private set;
        } = default(Func<Markdig.Syntax.ContainerBlock, Tsonic.CSharp.Js.JSArray<TocHeading>>)!;
        public static Action<Markdig.Syntax.ContainerBlock, Tsonic.CSharp.Js.JSArray<TocHeading>> collectHeadingsRecursive
        {
            get;
            private set;
        } = default(Action<Markdig.Syntax.ContainerBlock, Tsonic.CSharp.Js.JSArray<TocHeading>>)!;
        public static Func<string, string> escapeHtmlText
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static Func<string, string> generateTableOfContents
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_markdown_pipeline.__tsonic_module_init();
            indent = (int depth) =>
            {
                string @out = "";
                for (double i = 0; i < depth; i++)
                {
                    @out += "  ";
                }
                return @out;
            };
            appendInlinePlainText = (Markdig.Syntax.Inlines.Inline inline, System.Text.StringBuilder sb) =>
            {
                if (inline is Markdig.Syntax.Inlines.LiteralInline)
                {
                    Markdig.Syntax.Inlines.LiteralInline literal = (Markdig.Syntax.Inlines.LiteralInline)(Markdig.Syntax.Inlines.LiteralInline)inline;
                    sb.Append(literal.ToString());
                    return;
                }
                if (inline is Markdig.Syntax.Inlines.CodeInline)
                {
                    Markdig.Syntax.Inlines.CodeInline code = (Markdig.Syntax.Inlines.CodeInline)(Markdig.Syntax.Inlines.CodeInline)inline;
                    sb.Append(code.Content);
                    return;
                }
                if (inline is Markdig.Syntax.Inlines.HtmlEntityInline)
                {
                    Markdig.Syntax.Inlines.HtmlEntityInline entity = (Markdig.Syntax.Inlines.HtmlEntityInline)(Markdig.Syntax.Inlines.HtmlEntityInline)inline;
                    sb.Append(entity.Transcoded.ToString());
                    return;
                }
                if (inline is Markdig.Syntax.Inlines.AutolinkInline)
                {
                    Markdig.Syntax.Inlines.AutolinkInline autolink = (Markdig.Syntax.Inlines.AutolinkInline)(Markdig.Syntax.Inlines.AutolinkInline)inline;
                    sb.Append(autolink.Url);
                    return;
                }
                if (inline is Markdig.Syntax.Inlines.LineBreakInline)
                {
                    sb.Append(" ");
                    return;
                }
                if (inline is Markdig.Syntax.Inlines.ContainerInline)
                {
                    Markdig.Syntax.Inlines.ContainerInline container = (Markdig.Syntax.Inlines.ContainerInline)(Markdig.Syntax.Inlines.ContainerInline)inline;
                    Markdig.Syntax.Inlines.ContainerInline.Enumerator it = container.GetEnumerator();
                    while (it.MoveNext())
                    {
                        appendInlinePlainText(it.Current, sb);
                    }
                    it.Dispose();
                }
            };
            getHeadingPlainText = (Markdig.Syntax.HeadingBlock heading) =>
            {
                Markdig.Syntax.Inlines.ContainerInline? inline = heading.Inline;
                if (inline is null)
                {
                    return "";
                }
                System.Text.StringBuilder sb = new System.Text.StringBuilder();
                appendInlinePlainText(inline, sb);
                return sb.ToString();
            };
            collectHeadingsFromAst = (Markdig.Syntax.ContainerBlock document) =>
            {
                Tsonic.CSharp.Js.JSArray<TocHeading> headings = new Tsonic.CSharp.Js.JSArray<TocHeading>(new TocHeading[] { });
                collectHeadingsRecursive(document, headings);
                return headings;
            };
            collectHeadingsRecursive = (Markdig.Syntax.ContainerBlock container, Tsonic.CSharp.Js.JSArray<TocHeading> headings) =>
            {
                Markdig.Syntax.ContainerBlock.Enumerator it = container.GetEnumerator();
                while (it.MoveNext())
                {
                    Markdig.Syntax.Block block = it.Current;
                    if (block is Markdig.Syntax.HeadingBlock)
                    {
                        Markdig.Syntax.HeadingBlock heading = (Markdig.Syntax.HeadingBlock)(Markdig.Syntax.HeadingBlock)block;
                        Markdig.Renderers.Html.HtmlAttributes? attrs = Markdig.Renderers.Html.HtmlAttributesExtensions.TryGetAttributes(heading);
                        string id = attrs?.Id ?? "";
                        string text = getHeadingPlainText(heading);
                        headings.push(new TocHeading(heading.Level, text, id));
                    }
                    if (block is Markdig.Syntax.ContainerBlock)
                    {
                        collectHeadingsRecursive((Markdig.Syntax.ContainerBlock)(Markdig.Syntax.ContainerBlock)block, headings);
                    }
                }
                it.Dispose();
            };
            escapeHtmlText = (string text) =>
            {
                string result = text;
                result = Tsonic.CSharp.Js.String.replaceAll(result, "&", "&amp;");
                result = Tsonic.CSharp.Js.String.replaceAll(result, "<", "&lt;");
                result = Tsonic.CSharp.Js.String.replaceAll(result, ">", "&gt;");
                result = Tsonic.CSharp.Js.String.replaceAll(result, "\"", "&quot;");
                return result;
            };
            generateTableOfContents = (string markdown) =>
            {
                Markdig.Syntax.MarkdownDocument document = Markdig.Markdown.Parse(markdown, Node_modules_Tsumo_engine_src_markdown_pipeline.markdownPipeline);
                Tsonic.CSharp.Js.JSArray<TocHeading> headings = collectHeadingsFromAst(document);
                if (headings.length == 0)
                {
                    return "<nav id=\"TableOfContents\"></nav>";
                }
                System.Text.StringBuilder sb = new System.Text.StringBuilder();
                sb.Append("<nav id=\"TableOfContents\">\n");
                System.Collections.Generic.Stack<TocListFrame> listStack = new System.Collections.Generic.Stack<TocListFrame>();
                int currentLevel = 0;
                for (int i = 0; i < headings.length; i++)
                {
                    TocHeading h = headings[i];
                    int targetLevel = h.level;
                    if (currentLevel != 0 && targetLevel > currentLevel + 1)
                    {
                        targetLevel = currentLevel + 1;
                    }
                    if (listStack.Count == 0)
                    {
                        sb.Append($"{indent(1)}<ul>\n");
                        listStack.Push(new TocListFrame(targetLevel));
                        currentLevel = targetLevel;
                    }
                    while (listStack.Count > 0 && targetLevel < currentLevel)
                    {
                        TocListFrame top = listStack.Peek();
                        if (top.liOpen)
                        {
                            sb.Append($"{indent(listStack.Count + 1)}</li>\n");
                            top.liOpen = false;
                        }
                        sb.Append($"{indent(listStack.Count)}</ul>\n");
                        listStack.Pop();
                        currentLevel = listStack.Count > 0 ? listStack.Peek().level : 0;
                    }
                    if (listStack.Count == 0)
                    {
                        sb.Append($"{indent(1)}<ul>\n");
                        listStack.Push(new TocListFrame(targetLevel));
                        currentLevel = targetLevel;
                    }
                    if (targetLevel == currentLevel)
                    {
                        TocListFrame top_1 = listStack.Peek();
                        if (top_1.liOpen)
                        {
                            sb.Append($"{indent(listStack.Count + 1)}</li>\n");
                            top_1.liOpen = false;
                        }
                    }
                    if (targetLevel > currentLevel)
                    {
                        sb.Append($"{indent(listStack.Count + 1)}<ul>\n");
                        listStack.Push(new TocListFrame(targetLevel));
                        currentLevel = targetLevel;
                    }
                    sb.Append($"{indent(listStack.Count + 1)}<li><a href=\"#{h.id}\">{escapeHtmlText(h.text)}</a>\n");
                    listStack.Peek().liOpen = true;
                }
                while (listStack.Count > 0)
                {
                    TocListFrame top_2 = listStack.Peek();
                    if (top_2.liOpen)
                    {
                        sb.Append($"{indent(listStack.Count + 1)}</li>\n");
                        top_2.liOpen = false;
                    }
                    sb.Append($"{indent(listStack.Count)}</ul>\n");
                    listStack.Pop();
                }
                sb.Append("</nav>");
                return sb.ToString();
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class TocHeading
    {
        public int level;
        public string text;
        public string id;
        public TocHeading(int level, string text, string id)
        {
            this.level = level;
            this.text = text;
            this.id = id;
        }
    }
    public class TocListFrame
    {
        public int level;
        public bool liOpen;
        public TocListFrame(int level)
        {
            this.level = level;
            this.liOpen = false;
        }
    }
}
