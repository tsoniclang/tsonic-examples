using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_template_paths
    {
        public static Func<string, string> normalizeTemplateRelativePath
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static Func<string, string> templateDirectory
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static Action<Tsonic.CSharp.Js.JSArray<string>, string> pushUnique
        {
            get;
            private set;
        } = default(Action<Tsonic.CSharp.Js.JSArray<string>, string>)!;
        public static Func<string, string?, Tsonic.CSharp.Js.JSArray<string>> partialTemplateCandidates
        {
            get;
            private set;
        } = default(Func<string, string?, Tsonic.CSharp.Js.JSArray<string>>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            normalizeTemplateRelativePath = (string rawPath) =>
            {
                string normalized = Node_modules_Tsumo_engine_src_utils_strings.replaceText(Tsonic.CSharp.Js.String.trim(rawPath), "\\", "/");
                bool driveQualified = normalized.Length >= 2 && Node_modules_Tsumo_engine_src_utils_strings.substringCount(normalized, 1, 1) == ":";
                if (Tsonic.CSharp.Js.String.startsWith(normalized, "/") || driveQualified)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_PATH_ABSOLUTE", $"Template path must be layout-root relative: {rawPath}");
                }
                Tsonic.CSharp.Js.JSArray<string> segments = Tsonic.CSharp.Js.String.split(normalized, "/");
                Tsonic.CSharp.Js.JSArray<string> accepted = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                for (int index = 0; index < segments.length; index++)
                {
                    string segment = segments[index];
                    if (segment == "" || segment == ".")
                    {
                        continue;
                    }
                    if (segment == "..")
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_PATH_ESCAPES_ROOT", $"Template path escapes its layout root: {rawPath}");
                    }
                    if (Tsonic.CSharp.Js.String.includes(segment, "\0"))
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_PATH_INVALID", "Template path contains a null character");
                    }
                    accepted.push(segment);
                }
                return Tsonic.CSharp.Js.Array.join(accepted, "/");
            };
            templateDirectory = (string relativePath) =>
            {
                int lastSlash = Tsonic.CSharp.Js.String.lastIndexOf(relativePath, "/");
                return lastSlash < 0 ? "" : Node_modules_Tsumo_engine_src_utils_strings.substringCount(relativePath, 0, lastSlash);
            };
            pushUnique = (Tsonic.CSharp.Js.JSArray<string> values, string value) =>
            {
                for (int index = 0; index < values.length; index++)
                {
                    if (values[index] == value)
                    {
                        return;
                    }
                }
                values.push(value);
            };
            partialTemplateCandidates = (string nameRaw, string? callerRelativePath) =>
            {
                string name = normalizeTemplateRelativePath(nameRaw);
                if (name == "")
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_PARTIAL_NAME_EMPTY", "Template partial name cannot be empty");
                }
                Tsonic.CSharp.Js.JSArray<string> candidates = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                pushUnique(candidates, $"partials/{name}");
                pushUnique(candidates, $"_partials/{name}");
                if (callerRelativePath is not null)
                {
                    string selectedCallerPath = callerRelativePath;
                    string caller = normalizeTemplateRelativePath(selectedCallerPath);
                    string directory = templateDirectory(caller);
                    if (directory == "partials" || Tsonic.CSharp.Js.String.startsWith(directory, "partials/") || directory == "_partials" || Tsonic.CSharp.Js.String.startsWith(directory, "_partials/"))
                    {
                        pushUnique(candidates, normalizeTemplateRelativePath($"{directory}/{name}"));
                    }
                }
                return candidates;
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
