using System;

namespace Tsumo.Tests
{
    public static class Node_modules_Tsumo_engine_src_resources_glob
    {
        public static Func<string, string, bool> resourceSegmentMatches
        {
            get;
            private set;
        } = default(Func<string, string, bool>)!;
        public static Func<string, Tsonic.CSharp.Js.JSArray<string>> splitGlobSegments
        {
            get;
            private set;
        } = default(Func<string, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Func<Tsonic.CSharp.Js.JSArray<string>, Tsonic.CSharp.Js.JSArray<string>, int, int, bool> resourceGlobMatchesAt
        {
            get;
            private set;
        } = default(Func<Tsonic.CSharp.Js.JSArray<string>, Tsonic.CSharp.Js.JSArray<string>, int, int, bool>)!;
        public static Func<string, string, bool> resourceGlobMatches
        {
            get;
            private set;
        } = default(Func<string, string, bool>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_resources_paths.__tsonic_module_init();
            resourceSegmentMatches = (string pattern, string segment) =>
            {
                int braceStart = Tsonic.CSharp.Js.String.indexOf(pattern, "{");
                if (braceStart >= 0)
                {
                    int braceEnd = Tsonic.CSharp.Js.String.indexOf(pattern, "}", braceStart + 1);
                    if (braceEnd > braceStart + 1)
                    {
                        Tsonic.CSharp.Js.JSArray<string> alternatives = Tsonic.CSharp.Js.String.split(Tsonic.CSharp.Js.String.substring(pattern, braceStart + 1, braceEnd), ",");
                        if (alternatives.length > 1)
                        {
                            string prefix = Tsonic.CSharp.Js.String.substring(pattern, 0, braceStart);
                            string suffix = Tsonic.CSharp.Js.String.substring(pattern, braceEnd + 1);
                            for (int index = 0; index < alternatives.length; index++)
                            {
                                if (resourceSegmentMatches(prefix + alternatives[index] + suffix, segment))
                                {
                                    return true;
                                }
                            }
                            return false;
                        }
                    }
                }
                if (pattern == "*")
                {
                    return true;
                }
                int star = Tsonic.CSharp.Js.String.indexOf(pattern, "*");
                if (star < 0)
                {
                    return pattern == segment;
                }
                Tsonic.CSharp.Js.JSArray<string> parts = Tsonic.CSharp.Js.String.split(pattern, "*");
                int position = 0;
                for (int index_1 = 0; index_1 < parts.length; index_1++)
                {
                    string part = parts[index_1];
                    if (part == "")
                    {
                        continue;
                    }
                    int found = Tsonic.CSharp.Js.String.indexOf(segment, part, position);
                    if (found < 0)
                    {
                        return false;
                    }
                    if (index_1 == 0 && !Tsonic.CSharp.Js.String.startsWith(pattern, "*") && found != 0)
                    {
                        return false;
                    }
                    position = found + part.Length;
                }
                return Tsonic.CSharp.Js.String.endsWith(pattern, "*") || position == segment.Length;
            };
            splitGlobSegments = (string value) =>
            {
                string normalized = Node_modules_Tsumo_engine_src_resources_paths.normalizeResourceRelativePath(value);
                if (normalized == "")
                {
                    return new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                }
                return Tsonic.CSharp.Js.String.split(normalized, "/");
            };
            resourceGlobMatchesAt = (Tsonic.CSharp.Js.JSArray<string> patternSegments, Tsonic.CSharp.Js.JSArray<string> pathSegments, int patternIndex, int pathIndex) =>
            {
                if (patternIndex >= patternSegments.length)
                {
                    return pathIndex >= pathSegments.length;
                }
                string pattern = patternSegments[patternIndex];
                if (pattern == "**")
                {
                    for (int index = pathIndex; index <= pathSegments.length; index++)
                    {
                        if (resourceGlobMatchesAt(patternSegments, pathSegments, patternIndex + 1, index))
                        {
                            return true;
                        }
                    }
                    return false;
                }
                if (pathIndex >= pathSegments.length)
                {
                    return false;
                }
                if (!resourceSegmentMatches(pattern, pathSegments[pathIndex]))
                {
                    return false;
                }
                return resourceGlobMatchesAt(patternSegments, pathSegments, patternIndex + 1, pathIndex + 1);
            };
            resourceGlobMatches = (string pattern, string path) => resourceGlobMatchesAt(splitGlobSegments(pattern), splitGlobSegments(path), 0, 0);
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
