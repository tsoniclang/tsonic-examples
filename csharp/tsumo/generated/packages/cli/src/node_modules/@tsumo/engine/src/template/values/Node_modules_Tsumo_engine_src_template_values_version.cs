namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_template_values_version
    {
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_int32.__tsonic_module_init();
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
    public class VersionStringValue : TemplateValue
    {
        public string value;
        public VersionStringValue(string value) : base()
        {
            this.value = value;
        }
        public static int compare(string a, string b)
        {
            Tsonic.CSharp.Js.JSArray<int> aParts = VersionStringValue.parseVersion(a);
            Tsonic.CSharp.Js.JSArray<int> bParts = VersionStringValue.parseVersion(b);
            int aLen = aParts.length;
            int bLen = bParts.length;
            int maxLen = aLen > bLen ? aLen : bLen;
            for (int i = 0; i < maxLen; i++)
            {
                int av = i < aLen ? aParts[i] : 0;
                int bv = i < bLen ? bParts[i] : 0;
                if (av < bv)
                {
                    return -1;
                }
                if (av > bv)
                {
                    return 1;
                }
            }
            return 0;
        }
        public static Tsonic.CSharp.Js.JSArray<int> parseVersion(string v)
        {
            string cleaned = v;
            if (Tsonic.CSharp.Js.String.startsWith(cleaned, "v") || Tsonic.CSharp.Js.String.startsWith(cleaned, "V"))
            {
                cleaned = Node_modules_Tsumo_engine_src_utils_strings.substringFrom(cleaned, 1);
            }
            Tsonic.CSharp.Js.JSArray<string> parts = Tsonic.CSharp.Js.String.split(cleaned, ".");
            Tsonic.CSharp.Js.JSArray<int> result = new Tsonic.CSharp.Js.JSArray<int>(new int[] { });
            for (int i = 0; i < parts.length; i++)
            {
                string part = parts[i];
                int num = VersionStringValue.extractLeadingNumber(part);
                result.push(num);
            }
            return result;
        }
        public static int extractLeadingNumber(string s)
        {
            string numStr = "";
            for (int i = 0; i < s.Length; i++)
            {
                string ch = Node_modules_Tsumo_engine_src_utils_strings.substringCount(s, i, 1);
                if (Node_modules_Tsumo_engine_src_utils_strings.compareText(ch, "0") >= 0 && Node_modules_Tsumo_engine_src_utils_strings.compareText(ch, "9") <= 0)
                {
                    numStr = numStr + ch;
                }
                else
                {
                    break;
                }
            }
            if (numStr == "")
            {
                return 0;
            }
            int? value = Node_modules_Tsumo_engine_src_utils_int32.parseInt32(numStr);
            if (value is null)
            {
                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_VERSION_COMPONENT_OUT_OF_RANGE", $"Version component '{numStr}' is outside the supported int32 range");
            }
            return value.Value;
        }
    }
}
