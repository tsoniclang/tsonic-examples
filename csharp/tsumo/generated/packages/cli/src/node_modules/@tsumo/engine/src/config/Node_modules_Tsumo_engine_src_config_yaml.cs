using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_config_yaml
    {
        public static Func<string, int> indentationOf
        {
            get;
            private set;
        } = default(Func<string, int>)!;
        public static Func<string, string> yamlText
        {
            get;
            private set;
        } = default(Func<string, string>)!;
        public static Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>> splitPair
        {
            get;
            private set;
        } = default(Func<string, string?, int, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Action<Tsonic.CSharp.Js.Set<string>, string, string, string?, int> recordField
        {
            get;
            private set;
        } = default(Action<Tsonic.CSharp.Js.Set<string>, string, string, string?, int>)!;
        public static Action<MenuEntryBuilder, string, string, string?, int> applyMenuField
        {
            get;
            private set;
        } = default(Action<MenuEntryBuilder, string, string, string?, int>)!;
        public static Func<string, string?, SiteConfig> parseYamlConfig
        {
            get;
            private set;
        } = default(Func<string, string?, SiteConfig>)!;
        public static Func<SiteConfig, string, string, string?, SiteConfig> mergeYamlIntoConfig
        {
            get;
            private set;
        } = default(Func<SiteConfig, string, string, string?, SiteConfig>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_models.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_menus.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_params.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_structuredScalars.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_text.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_config_builders.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_config_scalars.__tsonic_module_init();
            indentationOf = (string line) =>
            {
                int indentation = 0;
                while (indentation < line.Length && line.Substring(indentation, 1) == " ")
                {
                    indentation++;
                }
                return indentation;
            };
            yamlText = (string line) => Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_structuredScalars.stripStructuredComment(line, "yaml"));
            splitPair = (string text, string? sourcePath, int line) =>
            {
                int separator = Tsonic.CSharp.Js.String.indexOf(text, ":");
                if (separator <= 0)
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "YAML configuration entries require 'key: value' syntax", sourcePath, line, 1);
                }
                return new Tsonic.CSharp.Js.JSArray<string>(new string[] { Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringCount(text, 0, separator)), Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringFrom(text, separator + 1)) });
            };
            recordField = (Tsonic.CSharp.Js.Set<string> fields, string field, string context, string? sourcePath, int line) =>
            {
                string normalized = Tsonic.CSharp.Js.String.toLowerCase(field);
                if (fields.has(normalized))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_DUPLICATE_FIELD", $"{context} field '{field}' is declared more than once", sourcePath, line, 1);
                }
                fields.add(normalized);
            };
            applyMenuField = (MenuEntryBuilder builder, string keyRaw, string value, string? sourcePath, int line) =>
            {
                string key = Tsonic.CSharp.Js.String.toLowerCase(keyRaw);
                if (key == "name")
                {
                    builder.name = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                }
                else
                {
                    if (key == "url")
                    {
                        builder.url = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                    }
                    else
                    {
                        if (key == "pageref")
                        {
                            builder.pageRef = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                        }
                        else
                        {
                            if (key == "title")
                            {
                                builder.title = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                            }
                            else
                            {
                                if (key == "parent")
                                {
                                    builder.parent = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                                }
                                else
                                {
                                    if (key == "identifier")
                                    {
                                        builder.identifier = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                                    }
                                    else
                                    {
                                        if (key == "pre")
                                        {
                                            builder.pre = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                                        }
                                        else
                                        {
                                            if (key == "post")
                                            {
                                                builder.post = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, line);
                                            }
                                            else
                                            {
                                                if (key == "weight")
                                                {
                                                    builder.weight = Node_modules_Tsumo_engine_src_config_scalars.parseConfigInt(keyRaw, value, "yaml", sourcePath, line);
                                                }
                                                else
                                                {
                                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_UNKNOWN_FIELD", $"Unknown menu configuration field '{keyRaw}'", sourcePath, line, 1);
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            parseYamlConfig = (string text, string? sourcePath) =>
            {
                string title = "Tsumo Site";
                string baseURL = "";
                string languageCode = "en-us";
                string contentDir = "content";
                string? theme = null;
                string? copyright = null;
                Tsonic.CSharp.Js.Map<string, ParamValue> @params = new Tsonic.CSharp.Js.Map<string, ParamValue>();
                Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>> menuBuilders = new Tsonic.CSharp.Js.Map<string, Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>>();
                Tsonic.CSharp.Js.Set<string> rootFields = new Tsonic.CSharp.Js.Set<string>();
                Tsonic.CSharp.Js.JSArray<string> lines = Tsonic.CSharp.Js.String.split(Tsonic.CSharp.Js.String.replaceAll(Tsonic.CSharp.Js.String.replaceAll(text, "\r\n", "\n"), "\r", "\n"), "\n");
                int index = 0;
                while (index < lines.length)
                {
                    string raw = lines[index];
                    int lineNumber = index + 1;
                    if (Tsonic.CSharp.Js.String.includes(raw, "\t"))
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "YAML configuration indentation must use spaces", sourcePath, lineNumber, 1);
                    }
                    string textValue = yamlText(raw);
                    if (textValue == "" || Tsonic.CSharp.Js.String.startsWith(textValue, "#"))
                    {
                        index++;
                        continue;
                    }
                    if (indentationOf(raw) != 0)
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "YAML configuration has an unexpected indented entry", sourcePath, lineNumber, 1);
                    }
                    Tsonic.CSharp.Js.JSArray<string> pair = splitPair(textValue, sourcePath, lineNumber);
                    string keyRaw = pair[0];
                    string key = Tsonic.CSharp.Js.String.toLowerCase(keyRaw);
                    string value = pair[1];
                    recordField(rootFields, keyRaw, "Configuration", sourcePath, lineNumber);
                    if (value != "")
                    {
                        if (key == "title")
                        {
                            title = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                        }
                        else
                        {
                            if (key == "baseurl")
                            {
                                baseURL = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                            }
                            else
                            {
                                if (key == "languagecode")
                                {
                                    languageCode = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                                }
                                else
                                {
                                    if (key == "contentdir")
                                    {
                                        contentDir = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                                    }
                                    else
                                    {
                                        if (key == "theme")
                                        {
                                            theme = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                                        }
                                        else
                                        {
                                            if (key == "copyright")
                                            {
                                                copyright = Node_modules_Tsumo_engine_src_config_scalars.parseConfigString(keyRaw, value, "yaml", sourcePath, lineNumber);
                                            }
                                            else
                                            {
                                                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_UNKNOWN_FIELD", $"Unknown configuration field '{keyRaw}'", sourcePath, lineNumber, 1);
                                            }
                                        }
                                    }
                                }
                            }
                        }
                        index++;
                        continue;
                    }
                    index++;
                    if (key == "params")
                    {
                        Tsonic.CSharp.Js.Set<string> paramFields = new Tsonic.CSharp.Js.Set<string>();
                        while (index < lines.length && indentationOf(lines[index]) > 0)
                        {
                            string childRaw = lines[index];
                            int childLine = index + 1;
                            string childText = yamlText(childRaw);
                            if (childText != "" && !Tsonic.CSharp.Js.String.startsWith(childText, "#"))
                            {
                                if (indentationOf(childRaw) != 2)
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "Configuration params require one scalar mapping level", sourcePath, childLine, 1);
                                }
                                Tsonic.CSharp.Js.JSArray<string> child = splitPair(childText, sourcePath, childLine);
                                if (child[1] == "")
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_INVALID_FIELD", $"Configuration param '{child[0]}' requires a scalar value", sourcePath, childLine, 1);
                                }
                                recordField(paramFields, child[0], "Configuration params", sourcePath, childLine);
                                @params.set(child[0], Node_modules_Tsumo_engine_src_config_scalars.parseConfigParam(child[1], "yaml", sourcePath, childLine));
                            }
                            index++;
                        }
                        continue;
                    }
                    if (key == "menu")
                    {
                        Tsonic.CSharp.Js.Set<string> menuNames = new Tsonic.CSharp.Js.Set<string>();
                        while (index < lines.length && indentationOf(lines[index]) > 0)
                        {
                            string menuRaw = lines[index];
                            int menuLine = index + 1;
                            string menuText = yamlText(menuRaw);
                            if (menuText == "" || Tsonic.CSharp.Js.String.startsWith(menuText, "#"))
                            {
                                index++;
                                continue;
                            }
                            if (indentationOf(menuRaw) != 2)
                            {
                                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "Menu names require one mapping level", sourcePath, menuLine, 1);
                            }
                            Tsonic.CSharp.Js.JSArray<string> menuPair = splitPair(menuText, sourcePath, menuLine);
                            if (menuPair[1] != "")
                            {
                                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_INVALID_FIELD", $"Menu '{menuPair[0]}' requires an array of entries", sourcePath, menuLine, 1);
                            }
                            string menuName = menuPair[0];
                            recordField(menuNames, menuName, "Configuration menu", sourcePath, menuLine);
                            Tsonic.CSharp.Js.JSArray<MenuEntryBuilder> entries = Tsonic.CSharp.Js.Map.getReference<string, Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>>(menuBuilders, menuName) ?? new Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>(new MenuEntryBuilder[] { });
                            index++;
                            while (index < lines.length && indentationOf(lines[index]) > 2)
                            {
                                string entryRaw = lines[index];
                                int entryLine = index + 1;
                                string entryText = yamlText(entryRaw);
                                if (entryText == "" || Tsonic.CSharp.Js.String.startsWith(entryText, "#"))
                                {
                                    index++;
                                    continue;
                                }
                                if (indentationOf(entryRaw) != 4 || !Tsonic.CSharp.Js.String.startsWith(entryText, "-"))
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", $"Menu '{menuName}' requires '- field: value' entries", sourcePath, entryLine, 1);
                                }
                                MenuEntryBuilder builder = new MenuEntryBuilder(menuName);
                                Tsonic.CSharp.Js.JSArray<string> first = splitPair(Tsonic.CSharp.Js.String.trim(Node_modules_Tsumo_engine_src_utils_strings.substringFrom(entryText, 1)), sourcePath, entryLine);
                                if (first[1] == "")
                                {
                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_INVALID_FIELD", $"Menu field '{first[0]}' requires a value", sourcePath, entryLine, 1);
                                }
                                Tsonic.CSharp.Js.Set<string> entryFields = new Tsonic.CSharp.Js.Set<string>();
                                recordField(entryFields, first[0], $"Menu '{menuName}' entry", sourcePath, entryLine);
                                applyMenuField(builder, first[0], first[1], sourcePath, entryLine);
                                index++;
                                while (index < lines.length && indentationOf(lines[index]) > 4)
                                {
                                    string fieldRaw = lines[index];
                                    int fieldLine = index + 1;
                                    string fieldText = yamlText(fieldRaw);
                                    if (fieldText != "" && !Tsonic.CSharp.Js.String.startsWith(fieldText, "#"))
                                    {
                                        if (indentationOf(fieldRaw) != 6)
                                        {
                                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "Menu entry fields require three mapping levels", sourcePath, fieldLine, 1);
                                        }
                                        Tsonic.CSharp.Js.JSArray<string> field = splitPair(fieldText, sourcePath, fieldLine);
                                        if (field[1] == "")
                                        {
                                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_INVALID_FIELD", $"Menu field '{field[0]}' requires a value", sourcePath, fieldLine, 1);
                                        }
                                        recordField(entryFields, field[0], $"Menu '{menuName}' entry", sourcePath, fieldLine);
                                        applyMenuField(builder, field[0], field[1], sourcePath, fieldLine);
                                    }
                                    index++;
                                }
                                entries.push(builder);
                            }
                            menuBuilders.set(menuName, entries);
                        }
                        continue;
                    }
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_UNKNOWN_FIELD", $"Unknown nested configuration field '{keyRaw}'", sourcePath, lineNumber, 1);
                }
                SiteConfig config = new SiteConfig(title, Node_modules_Tsumo_engine_src_utils_text.ensureTrailingSlash(baseURL), languageCode, theme, copyright);
                config.contentDir = contentDir;
                config.Params = @params;
                foreach (string menuName_1 in menuBuilders.keys())
                {
                    Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>? builders = Tsonic.CSharp.Js.Map.getReference<string, Tsonic.CSharp.Js.JSArray<MenuEntryBuilder>>(menuBuilders, menuName_1);
                    if (builders is null)
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_MODEL_INCONSISTENT", $"Menu '{menuName_1}' disappeared during configuration finalization", sourcePath);
                    }
                    Tsonic.CSharp.Js.JSArray<MenuEntry> entries_1 = new Tsonic.CSharp.Js.JSArray<MenuEntry>(new MenuEntry[] { });
                    for (int index_1 = 0; index_1 < builders.length; index_1++)
                    {
                        entries_1.push(builders[index_1].toEntry());
                    }
                    config.Menus.set(menuName_1, Node_modules_Tsumo_engine_src_menus.buildMenuHierarchy(entries_1));
                }
                return config;
            };
            mergeYamlIntoConfig = (SiteConfig config, string text, string fileName, string? sourcePath) =>
            {
                string lower = Tsonic.CSharp.Js.String.toLowerCase(fileName);
                if (lower == "hugo.yaml" || lower == "hugo.yml" || lower == "config.yaml" || lower == "config.yml")
                {
                    return parseYamlConfig(text, sourcePath);
                }
                if (lower == "params.yaml" || lower == "params.yml")
                {
                    Tsonic.CSharp.Js.JSArray<string> lines = Tsonic.CSharp.Js.String.split(Tsonic.CSharp.Js.String.replaceAll(Tsonic.CSharp.Js.String.replaceAll(text, "\r\n", "\n"), "\r", "\n"), "\n");
                    Tsonic.CSharp.Js.Set<string> fields = new Tsonic.CSharp.Js.Set<string>();
                    for (int index = 0; index < lines.length; index++)
                    {
                        string raw = lines[index];
                        string value = yamlText(raw);
                        if (value == "" || Tsonic.CSharp.Js.String.startsWith(value, "#"))
                        {
                            continue;
                        }
                        if (indentationOf(raw) != 0)
                        {
                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_SYNTAX_INVALID", "Split params configuration requires a flat scalar mapping", sourcePath, index + 1, 1);
                        }
                        Tsonic.CSharp.Js.JSArray<string> pair = splitPair(value, sourcePath, index + 1);
                        if (pair[1] == "")
                        {
                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_INVALID_FIELD", $"Configuration param '{pair[0]}' requires a scalar value", sourcePath, index + 1, 1);
                        }
                        recordField(fields, pair[0], "Configuration params", sourcePath, index + 1);
                        config.Params.set(pair[0], Node_modules_Tsumo_engine_src_config_scalars.parseConfigParam(pair[1], "yaml", sourcePath, index + 1));
                    }
                    return config;
                }
                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_CONFIG_FILE_UNSUPPORTED", $"Unsupported split configuration file '{fileName}'", sourcePath);
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
