using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_utils_regularExpressions
    {
        public static Func<string, string, int, Tsonic.CSharp.Js.JSArray<string>> findRegularExpressionMatches
        {
            get;
            private set;
        } = default(Func<string, string, int, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Func<string, string, int, Tsonic.CSharp.Js.JSArray<Tsonic.CSharp.Js.JSArray<string>>> findRegularExpressionSubmatches
        {
            get;
            private set;
        } = default(Func<string, string, int, Tsonic.CSharp.Js.JSArray<Tsonic.CSharp.Js.JSArray<string>>>)!;
        public static Func<string, string, string, int, string> replaceRegularExpression
        {
            get;
            private set;
        } = default(Func<string, string, string, int, string>)!;
        internal static Tsonic.CSharp.Js.RegExp compileRegularExpression(string pattern)
        {
            try
            {
                return new Tsonic.CSharp.Js.RegExp(pattern, "g");
            }
            catch
            {
                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_REGEXP_INVALID", $"Invalid regular expression '{pattern}'");
            }
        }
        internal static string expandRegularExpressionReplacement(string replacement, string input, Tsonic.CSharp.Js.RegExpExecArray match, string fullMatch, int matchIndex)
        {
            Tsonic.CSharp.Js.JSArray<string> result = Tsonic.CSharp.Js.JSArray<string>.of([]);
            for (int index = 0; index < replacement.Length; index++)
            {
                string current = Tsonic.CSharp.Js.String.charAt(replacement, index);
                if (current != "$" || index + 1 >= replacement.Length)
                {
                    result.push(current);
                    continue;
                }
                string next = Tsonic.CSharp.Js.String.charAt(replacement, index + 1);
                if (next == "$")
                {
                    result.push("$");
                    index++;
                    continue;
                }
                if (next == "&")
                {
                    result.push(fullMatch);
                    index++;
                    continue;
                }
                if (next == "`")
                {
                    result.push(Tsonic.CSharp.Js.String.slice(input, 0, matchIndex));
                    index++;
                    continue;
                }
                if (next == "'")
                {
                    result.push(Tsonic.CSharp.Js.String.slice(input, matchIndex + fullMatch.Length));
                    index++;
                    continue;
                }
                if (next == "<" && match.groups is not null)
                {
                    int closing = Tsonic.CSharp.Js.String.indexOf(replacement, ">", index + 2);
                    if (closing >= 0)
                    {
                        string groupName = Tsonic.CSharp.Js.String.slice(replacement, index + 2, closing);
                        result.push(regularExpressionNamedGroup(match, groupName));
                        index = closing;
                        continue;
                    }
                }
                int firstDigit = digitValue(next);
                if (firstDigit >= 0)
                {
                    int captureIndex = -1;
                    int consumedDigits = 0;
                    if (index + 2 < replacement.Length)
                    {
                        int secondDigit = digitValue(Tsonic.CSharp.Js.String.charAt(replacement, index + 2));
                        int twoDigitIndex = firstDigit * 10 + secondDigit;
                        if (secondDigit >= 0 && twoDigitIndex > 0 && twoDigitIndex < match.length)
                        {
                            captureIndex = twoDigitIndex;
                            consumedDigits = 2;
                        }
                    }
                    if (captureIndex < 0 && firstDigit > 0 && firstDigit < match.length)
                    {
                        captureIndex = firstDigit;
                        consumedDigits = 1;
                    }
                    if (captureIndex > 0)
                    {
                        result.push(match[captureIndex] ?? "");
                        index += consumedDigits;
                        continue;
                    }
                }
                result.push("$");
            }
            return Tsonic.CSharp.Js.Array.join(result, "");
        }
        internal static string regularExpressionNamedGroup(Tsonic.CSharp.Js.RegExpExecArray match, string groupName)
        {
            return match.groups?[groupName] ?? "";
        }
        internal static string requireFullMatch(Tsonic.CSharp.Js.RegExpExecArray match)
        {
            string? fullMatch = match[0];
            if (fullMatch == null)
            {
                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_REGEXP_RESULT_INVALID", "Regular expression execution returned no full match");
            }
            return fullMatch;
        }
        internal static int digitValue(string value)
        {
            if (value.Length != 1)
            {
                return -1;
            }
            int code = (int)Tsonic.CSharp.Js.String.charCodeAt(value, 0);
            return code >= 48 && code <= 57 ? code - 48 : -1;
        }
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            findRegularExpressionMatches = (string pattern, string input, int limit) =>
            {
                Tsonic.CSharp.Js.RegExp expression = compileRegularExpression(pattern);
                if (limit == 0)
                {
                    return Tsonic.CSharp.Js.JSArray<string>.of([]);
                }
                Tsonic.CSharp.Js.JSArray<string> result = Tsonic.CSharp.Js.JSArray<string>.of([]);
                foreach (Tsonic.CSharp.Js.RegExpExecArray match in Tsonic.CSharp.Js.String.matchAll(input, expression))
                {
                    result.push(requireFullMatch(match));
                    if (limit > 0 && result.length >= limit)
                    {
                        break;
                    }
                }
                return result;
            };
            findRegularExpressionSubmatches = (string pattern, string input, int limit) =>
            {
                Tsonic.CSharp.Js.RegExp expression = compileRegularExpression(pattern);
                if (limit == 0)
                {
                    return Tsonic.CSharp.Js.JSArray<Tsonic.CSharp.Js.JSArray<string>>.of([]);
                }
                Tsonic.CSharp.Js.JSArray<Tsonic.CSharp.Js.JSArray<string>> result = Tsonic.CSharp.Js.JSArray<Tsonic.CSharp.Js.JSArray<string>>.of([]);
                foreach (Tsonic.CSharp.Js.RegExpExecArray match in Tsonic.CSharp.Js.String.matchAll(input, expression))
                {
                    Tsonic.CSharp.Js.JSArray<string> row = Tsonic.CSharp.Js.JSArray<string>.of([requireFullMatch(match)]);
                    for (double groupIndex = 1; groupIndex < match.length; groupIndex++)
                    {
                        row.push(match[groupIndex] ?? "");
                    }
                    result.push(row);
                    if (limit > 0 && result.length >= limit)
                    {
                        break;
                    }
                }
                return result;
            };
            replaceRegularExpression = (string pattern, string replacement, string input, int limit) =>
            {
                Tsonic.CSharp.Js.RegExp expression = compileRegularExpression(pattern);
                if (limit == 0)
                {
                    return input;
                }
                if (limit < 0)
                {
                    return Tsonic.CSharp.Js.String.replace(input, expression, replacement);
                }
                Tsonic.CSharp.Js.JSArray<string> result = Tsonic.CSharp.Js.JSArray<string>.of([]);
                int cursor = 0;
                int remaining = limit;
                foreach (Tsonic.CSharp.Js.RegExpExecArray match in Tsonic.CSharp.Js.String.matchAll(input, expression))
                {
                    if (remaining == 0)
                    {
                        break;
                    }
                    int matchIndex = (int)match.index;
                    string fullMatch = requireFullMatch(match);
                    result.push(Tsonic.CSharp.Js.String.slice(input, cursor, matchIndex));
                    result.push(expandRegularExpressionReplacement(replacement, input, match, fullMatch, matchIndex));
                    cursor = matchIndex + fullMatch.Length;
                    remaining--;
                }
                result.push(Tsonic.CSharp.Js.String.slice(input, cursor));
                return Tsonic.CSharp.Js.Array.join(result, "");
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
