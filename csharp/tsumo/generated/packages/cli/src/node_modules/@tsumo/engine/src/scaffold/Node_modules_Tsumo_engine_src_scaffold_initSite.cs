using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_scaffold_initSite
    {
        internal static void ensureEmptyDir(string path)
        {
            if (!Node_modules_Tsumo_engine_src_fs.dirExists(path))
            {
                Node_modules_Tsumo_engine_src_fs.ensureDir(path);
                return;
            }
            Node_modules_Tsumo_engine_src_fs.rejectFilesystemLink(path);
            if (Tsonic.CSharp.Node.fs.readdirSync(path).Length > 0)
            {
                throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_SCAFFOLD_DESTINATION_NOT_EMPTY", $"Directory not empty: {path}", path);
            }
        }
        internal static string defaultConfigToml(string title)
        {
            return $"baseURL = \"http://localhost:1313/\"\nlanguageCode = \"en-us\"\ntitle = \"{title}\"\n";
        }
        internal static string defaultArchetype()
        {
            return """
            ---
            title: "{{ .Title }}"
            date: "{{ .Date }}"
            draft: true
            description: ""
            tags: []
            categories: []
            ---

            Write your post here.

            """;
        }
        internal static string baseofHtml()
        {
            return """
            <!doctype html>
            <html lang="{{ .Site.LanguageCode }}">
              <head>
                <meta charset="utf-8" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <title>{{ .Title }} | {{ .Site.Title }}</title>
                <meta name="description" content="{{ default .Site.Title .Description }}" />
                <link rel="stylesheet" href="{{ relURL "style.css" }}" />
                <link rel="alternate" type="application/rss+xml" href="{{ relURL "/index.xml" }}" title="{{ .Site.Title }}" />
              </head>
              <body>
                {{ partial "header.html" . }}
                <main class="container">
                  {{ block "main" . }}{{ end }}
                </main>
                {{ partial "footer.html" . }}
              </body>
            </html>

            """;
        }
        internal static string partialHeader()
        {
            return """
            <header class="container">
              <h1><a href="{{ relURL "/" }}">{{ .Site.Title }}</a></h1>
              <nav>
                <a href="{{ relURL "/" }}">Home</a>
                <a href="{{ relURL "/posts/" }}">Posts</a>
                <a href="{{ relURL "/tags/" }}">Tags</a>
                <a href="{{ relURL "/categories/" }}">Categories</a>
              </nav>
            </header>

            """;
        }
        internal static string partialFooter()
        {
            return """
            <footer class="container">
              <p class="muted">Built with tsumo</p>
            </footer>

            """;
        }
        internal static string singleHtml()
        {
            return """
            {{ define "main" }}
            <article>
              <h2>{{ .Title }}</h2>
              <p class="muted">
                {{ dateFormat "Jan 2, 2006" .Date }}
                {{ with .Categories }}
                  · Categories:
                  {{ range . }}
                    <a href="{{ . | urlize | printf "/categories/%s/" | relURL }}">{{ . }}</a>
                  {{ end }}
                {{ end }}
                {{ with .Tags }}
                  · Tags:
                  {{ range . }}
                    <a href="{{ . | urlize | printf "/tags/%s/" | relURL }}">{{ . }}</a>
                  {{ end }}
                {{ end }}
              </p>
              <div class="content">
                {{ .Content }}
              </div>
            </article>
            {{ end }}

            """;
        }
        internal static string listHtml()
        {
            return """
            {{ define "main" }}
            <section>
              <h2>{{ .Title }}</h2>
              <div class="content">{{ .Content }}</div>
              <ul class="post-list">
                {{ range .Pages }}
                  <li>
                    <div>
                      <a href="{{ .RelPermalink }}">{{ .Title }}</a>
                      {{ with .Summary }}<div class="summary">{{ . }}</div>{{ end }}
                    </div>
                    <span class="muted">{{ dateFormat "Jan 2, 2006" .Date }}</span>
                  </li>
                {{ end }}
              </ul>
            </section>
            {{ end }}

            """;
        }
        internal static string termsHtml()
        {
            return """
            {{ define "main" }}
            <section>
              <h2>{{ .Title }}</h2>
              <ul class="post-list">
                {{ range .Pages }}
                  <li>
                    <a href="{{ .RelPermalink }}">{{ .Title }}</a>
                    <span class="muted">{{ len .Pages }}</span>
                  </li>
                {{ end }}
              </ul>
            </section>
            {{ end }}

            """;
        }
        internal static string taxonomyHtml()
        {
            return """
            {{ define "main" }}
            <section>
              <h2>{{ .Title }}</h2>
              <ul class="post-list">
                {{ range .Pages }}
                  <li>
                    <a href="{{ .RelPermalink }}">{{ .Title }}</a>
                    <span class="muted">{{ dateFormat "Jan 2, 2006" .Date }}</span>
                  </li>
                {{ end }}
              </ul>
            </section>
            {{ end }}

            """;
        }
        internal static string indexMd()
        {
            return """
            ---
            title: "Home"
            description: "Example site for tsumo."
            ---

            Welcome to your new site.

            """;
        }
        internal static string helloWorldMd(Tsonic.CSharp.Js.Date creationTime)
        {
            return $"---\ntitle: \"Hello World\"\ndate: \"{creationTime.toISOString()}\"\ndraft: false\ndescription: \"An end-to-end demo of tsumo with GFM markdown.\"\ntags: [\"hello\", \"tsumo\", \"gfm\"]\ncategories: [\"meta\"]\n---\n\nThis is your first post.\n\n<!--more-->\n\n```\ntsumo build\ntsumo server\n```\n";
        }
        internal static string styleCss()
        {
            return """
            :root { color-scheme: light dark; }
            body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 0; line-height: 1.5; }
            a { color: inherit; }
            .container { max-width: 860px; margin: 0 auto; padding: 1.25rem; }
            .muted { color: #777; }
            nav { display: flex; gap: 1rem; flex-wrap: wrap; }
            .post-list { list-style: none; padding: 0; }
            .post-list li { display: flex; justify-content: space-between; gap: 1rem; padding: 0.25rem 0; }
            .summary { margin-top: 0.25rem; }
            .summary p { margin: 0; }
            .content pre { padding: 0.75rem 1rem; background: rgba(127,127,127,0.15); overflow: auto; border-radius: 10px; }

            """;
        }
        public static Action<string, Tsonic.CSharp.Js.Date?> initSite
        {
            get;
            private set;
        } = default(Action<string, Tsonic.CSharp.Js.Date?>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_fs.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_text.__tsonic_module_init();
            initSite = (string targetDir, Tsonic.CSharp.Js.Date? creationTime) =>
            {
                string dir = Tsonic.CSharp.Node.path.resolve(targetDir);
                Tsonic.CSharp.Js.Date scaffoldTime = creationTime ?? new Tsonic.CSharp.Js.Date();
                ensureEmptyDir(dir);
                string @base = Tsonic.CSharp.Node.path.basename(dir);
                string title = Node_modules_Tsumo_engine_src_utils_text.humanizeSlug(@base == "" ? "Tsumo Site" : @base);
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "content"));
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "content", "posts"));
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default"));
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "layouts", "partials"));
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "static"));
                Node_modules_Tsumo_engine_src_fs.ensureDir(Tsonic.CSharp.Node.path.join(dir, "archetypes"));
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "hugo.toml"), defaultConfigToml(title));
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "archetypes", "default.md"), defaultArchetype());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default", "baseof.html"), baseofHtml());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default", "single.html"), singleHtml());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default", "list.html"), listHtml());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default", "terms.html"), termsHtml());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "_default", "taxonomy.html"), taxonomyHtml());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "partials", "header.html"), partialHeader());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "layouts", "partials", "footer.html"), partialFooter());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "static", "style.css"), styleCss());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "content", "_index.md"), indexMd());
                Node_modules_Tsumo_engine_src_fs.writeTextFile(Tsonic.CSharp.Node.path.join(dir, "content", "posts", "hello-world.md"), helloWorldMd(scaffoldTime));
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
