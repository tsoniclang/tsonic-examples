using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_template_parser_stringLiterals
    {
        public static TsumoError invalidStringLiteral(string message)
        {
            return Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_TEMPLATE_STRING_ESCAPE_INVALID", message);
        }
        public static Func<string, int, int> digitValue
        {
            get;
            private set;
        } = default(Func<string, int, int>)!;
        public static Func<string, int, int, int, int, string, string> decodeFixedEscape
        {
            get;
            private set;
        } = default(Func<string, int, int, int, int, string, string>)!;
        public static Func<string, string, string> decodeInterpretedString
        {
            get;
            private set;
        } = default(Func<string, string, string>)!;
        public static Func<string, string?> decodeTemplateStringLiteral
        {
            get;
            private set;
        } = default(Func<string, string?>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_strings.__tsonic_module_init();
            digitValue = (string character, int radix) =>
            {
                int value = Node_modules_Tsumo_engine_src_utils_strings.indexOfText("0123456789abcdef", Tsonic.CSharp.Js.String.toLowerCase(character));
                return value >= 0 && value < radix ? value : -1;
            };
            decodeFixedEscape = (string source, int start, int count, int radix, int maximum, string description) =>
            {
                if (start + count > source.Length)
                {
                    throw invalidStringLiteral($"{description} requires exactly {count} digits");
                }
                int value = 0;
                for (int offset = 0; offset < count; offset++)
                {
                    int digit = digitValue(Node_modules_Tsumo_engine_src_utils_strings.codePointAtText(source, start + offset), radix);
                    if (digit < 0)
                    {
                        throw invalidStringLiteral($"{description} contains an invalid digit");
                    }
                    int maximumBeforeDigit = (int)Tsonic.CSharp.Js.Math.floor((maximum - digit) / radix);
                    if (value > maximumBeforeDigit)
                    {
                        throw invalidStringLiteral($"{description} is outside its valid range");
                    }
                    value = value * radix + digit;
                }
                if (value >= 55296 && value <= 57343)
                {
                    throw invalidStringLiteral($"{description} does not name a Unicode scalar value");
                }
                return Tsonic.CSharp.Js.String.fromCodePoint(value);
            };
            decodeInterpretedString = (string inner, string quote) =>
            {
                string result = "";
                int index = 0;
                while (index < inner.Length)
                {
                    string current = Node_modules_Tsumo_engine_src_utils_strings.codePointAtText(inner, index);
                    if (current == "\n" || current == "\r")
                    {
                        throw invalidStringLiteral("Interpreted template strings cannot contain unescaped line breaks");
                    }
                    if (current != "\\")
                    {
                        result += current;
                        index = Node_modules_Tsumo_engine_src_utils_strings.nextCodePointIndex(inner, index);
                        continue;
                    }
                    int escapeIndex = Node_modules_Tsumo_engine_src_utils_strings.nextCodePointIndex(inner, index);
                    if (escapeIndex >= inner.Length)
                    {
                        throw invalidStringLiteral("Template string ends with an incomplete escape");
                    }
                    string escaped = Node_modules_Tsumo_engine_src_utils_strings.codePointAtText(inner, escapeIndex);
                    if (escaped == quote || escaped == "\\")
                    {
                        result += escaped;
                        index = Node_modules_Tsumo_engine_src_utils_strings.nextCodePointIndex(inner, escapeIndex);
                        continue;
                    }
                    if (escaped == "a")
                    {
                        result += "\u0007";
                    }
                    else
                    {
                        if (escaped == "b")
                        {
                            result += "\b";
                        }
                        else
                        {
                            if (escaped == "f")
                            {
                                result += "\f";
                            }
                            else
                            {
                                if (escaped == "n")
                                {
                                    result += "\n";
                                }
                                else
                                {
                                    if (escaped == "r")
                                    {
                                        result += "\r";
                                    }
                                    else
                                    {
                                        if (escaped == "t")
                                        {
                                            result += "\t";
                                        }
                                        else
                                        {
                                            if (escaped == "v")
                                            {
                                                result += "\v";
                                            }
                                            else
                                            {
                                                if (digitValue(escaped, 8) >= 0)
                                                {
                                                    result += decodeFixedEscape(inner, escapeIndex, 3, 8, 255, "Octal template string escape");
                                                    index = escapeIndex + 3;
                                                    continue;
                                                }
                                                else
                                                {
                                                    if (escaped == "x")
                                                    {
                                                        result += decodeFixedEscape(inner, escapeIndex + 1, 2, 16, 255, "Hexadecimal template string escape");
                                                        index = escapeIndex + 3;
                                                        continue;
                                                    }
                                                    else
                                                    {
                                                        if (escaped == "u")
                                                        {
                                                            result += decodeFixedEscape(inner, escapeIndex + 1, 4, 16, 1114111, "Unicode template string escape");
                                                            index = escapeIndex + 5;
                                                            continue;
                                                        }
                                                        else
                                                        {
                                                            if (escaped == "U")
                                                            {
                                                                result += decodeFixedEscape(inner, escapeIndex + 1, 8, 16, 1114111, "Unicode template string escape");
                                                                index = escapeIndex + 9;
                                                                continue;
                                                            }
                                                            else
                                                            {
                                                                throw invalidStringLiteral($"Unsupported template string escape '\\{escaped}'");
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                    index = Node_modules_Tsumo_engine_src_utils_strings.nextCodePointIndex(inner, escapeIndex);
                }
                return result;
            };
            decodeTemplateStringLiteral = (string token) =>
            {
                string value = Tsonic.CSharp.Js.String.trim(token);
                if (value.Length < 2)
                {
                    return null;
                }
                string quote = Node_modules_Tsumo_engine_src_utils_strings.codePointAtText(value, 0);
                if (quote != "\"" && quote != "'" && quote != "`")
                {
                    return null;
                }
                if (!Tsonic.CSharp.Js.String.endsWith(value, quote))
                {
                    return null;
                }
                string inner = Node_modules_Tsumo_engine_src_utils_strings.substringCount(value, 1, value.Length - 2);
                return quote == "`" ? Tsonic.CSharp.Js.String.replaceAll(inner, "\r", "") : decodeInterpretedString(inner, quote);
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
