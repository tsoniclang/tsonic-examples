using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_serveSite
    {
        public static Action<string> logLine
        {
            get;
            private set;
        } = default(Action<string>)!;
        public static Action<string> logErrorLine
        {
            get;
            private set;
        } = default(Action<string>)!;
        public static Action<Tsonic.CSharp.Node.Http.ServerResponse, int, string, string> sendText
        {
            get;
            private set;
        } = default(Action<Tsonic.CSharp.Node.Http.ServerResponse, int, string, string>)!;
        public static Action<Tsonic.CSharp.Node.Http.ServerResponse, int, string, Tsonic.CSharp.Node.Buffer> sendBytes
        {
            get;
            private set;
        } = default(Action<Tsonic.CSharp.Node.Http.ServerResponse, int, string, Tsonic.CSharp.Node.Buffer>)!;
        public static Func<string, bool> isTextLikeContentType
        {
            get;
            private set;
        } = default(Func<string, bool>)!;
        public static Func<Tsonic.CSharp.Node.Http.IncomingMessage, string> getRequestPath
        {
            get;
            private set;
        } = default(Func<Tsonic.CSharp.Node.Http.IncomingMessage, string>)!;
        public static Func<string, string, string?, string?> safeResolveUnderRoot
        {
            get;
            private set;
        } = default(Func<string, string, string?, string?>)!;
        public static Func<string, string, string?> resolveRequestPath
        {
            get;
            private set;
        } = default(Func<string, string, string?>)!;
        public static Action<string, Tsonic.CSharp.Node.Http.IncomingMessage, Tsonic.CSharp.Node.Http.ServerResponse> handleRequest
        {
            get;
            private set;
        } = default(Action<string, Tsonic.CSharp.Node.Http.IncomingMessage, Tsonic.CSharp.Node.Http.ServerResponse>)!;
        public static Func<ServeRequest, Tsonic.CSharp.Js.JSArray<string>> collectWatchTargets
        {
            get;
            private set;
        } = default(Func<ServeRequest, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Action<ServeRequest, Action<string>> startWatchLoop
        {
            get;
            private set;
        } = default(Action<ServeRequest, Action<string>>)!;
        public static Action<ServeRequest> serveSite
        {
            get;
            private set;
        } = default(Action<ServeRequest>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_buildSite.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_docs_config.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_fs.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_models.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_mime.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_text.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_watchSnapshot.__tsonic_module_init();
            logLine = (string message) =>
            {
                Tsonic.CSharp.Js.console.log(message);
            };
            logErrorLine = (string message) =>
            {
                Tsonic.CSharp.Js.console.error(message);
            };
            sendText = (Tsonic.CSharp.Node.Http.ServerResponse response, int statusCode, string contentType, string body) =>
            {
                response.statusCode = statusCode;
                response.setHeader("Content-Type", contentType);
                response.end(body);
            };
            sendBytes = (Tsonic.CSharp.Node.Http.ServerResponse response, int statusCode, string contentType, Tsonic.CSharp.Node.Buffer bytes) =>
            {
                response.statusCode = statusCode;
                response.setHeader("Content-Type", contentType);
                response.end(bytes);
            };
            isTextLikeContentType = (string contentType) =>
            {
                return (Tsonic.CSharp.Js.String.startsWith(contentType, "text/") || Tsonic.CSharp.Js.String.startsWith(contentType, "application/json") || Tsonic.CSharp.Js.String.startsWith(contentType, "application/xml") || Tsonic.CSharp.Js.String.endsWith(contentType, "+xml"));
            };
            getRequestPath = (Tsonic.CSharp.Node.Http.IncomingMessage request) =>
            {
                string raw = request.url ?? "/";
                int queryIndex = Tsonic.CSharp.Js.String.indexOf(raw, "?");
                int hashIndex = Tsonic.CSharp.Js.String.indexOf(raw, "#");
                int end = raw.Length;
                if (queryIndex >= 0 && queryIndex < end)
                {
                    end = queryIndex;
                }
                if (hashIndex >= 0 && hashIndex < end)
                {
                    end = hashIndex;
                }
                string path = Tsonic.CSharp.Js.String.substring(raw, 0, end);
                return path == "" ? "/" : path;
            };
            safeResolveUnderRoot = (string rootDir, string requestPath, string? suffixRaw) =>
            {
                string? suffix = suffixRaw;
                string rootFull = Tsonic.CSharp.Node.path.resolve(rootDir);
                string prefix = Tsonic.CSharp.Js.String.endsWith(rootFull, Tsonic.CSharp.Node.path.sep) ? rootFull : rootFull + Tsonic.CSharp.Node.path.sep;
                string candidate = suffix is null ? Tsonic.CSharp.Node.path.resolve(rootFull, "." + requestPath) : Tsonic.CSharp.Node.path.resolve(rootFull, "." + requestPath, suffix);
                if (candidate != rootFull && !Tsonic.CSharp.Js.String.startsWith(candidate, prefix))
                {
                    return null;
                }
                return candidate;
            };
            resolveRequestPath = (string outDir, string requestPath) =>
            {
                if (requestPath == "/" || Tsonic.CSharp.Js.String.endsWith(requestPath, "/"))
                {
                    string? indexPath = safeResolveUnderRoot(outDir, requestPath, "index.html");
                    return indexPath is not null && Node_modules_Tsumo_engine_src_fs.fileExists(indexPath) ? indexPath : null;
                }
                string? directPath = safeResolveUnderRoot(outDir, requestPath, null);
                if (directPath is not null && Node_modules_Tsumo_engine_src_fs.fileExists(directPath))
                {
                    return directPath;
                }
                if (Tsonic.CSharp.Node.path.extname(requestPath) == "")
                {
                    string? indexPath_1 = safeResolveUnderRoot(outDir, requestPath, "index.html");
                    if (indexPath_1 is not null && Node_modules_Tsumo_engine_src_fs.fileExists(indexPath_1))
                    {
                        return indexPath_1;
                    }
                }
                return null;
            };
            handleRequest = (string outDir, Tsonic.CSharp.Node.Http.IncomingMessage request, Tsonic.CSharp.Node.Http.ServerResponse response) =>
            {
                string requestPath = getRequestPath(request);
                string? filePath = resolveRequestPath(outDir, requestPath);
                if (filePath is null)
                {
                    sendText(response, 404, "text/plain; charset=utf-8", "Not Found");
                    return;
                }
                string contentType = Node_modules_Tsumo_engine_src_utils_mime.contentTypeForPath(filePath);
                if (isTextLikeContentType(contentType))
                {
                    sendText(response, 200, contentType, Node_modules_Tsumo_engine_src_fs.readTextFile(filePath));
                    return;
                }
                sendBytes(response, 200, contentType, Node_modules_Tsumo_engine_src_fs.readBinaryFile(filePath));
            };
            collectWatchTargets = (ServeRequest req) =>
            {
                string siteDir = Tsonic.CSharp.Node.path.resolve(req.siteDir);
                Tsonic.CSharp.Js.JSArray<string> targets = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                LoadedDocsConfig? docsConfig = Node_modules_Tsumo_engine_src_docs_config.loadDocsConfig(siteDir);
                if (docsConfig is null)
                {
                    targets.push(Tsonic.CSharp.Node.path.resolve(siteDir, "content"));
                    targets.push(Tsonic.CSharp.Node.path.resolve(siteDir, "archetypes"));
                }
                else
                {
                    Tsonic.CSharp.Js.JSArray<DocsMountConfig> mounts = docsConfig.config.mounts;
                    for (int i = 0; i < mounts.length; i++)
                    {
                        targets.push(Tsonic.CSharp.Node.path.resolve(mounts[i].sourceDir));
                    }
                    targets.push(Tsonic.CSharp.Node.path.resolve(siteDir, "tsumo.docs.json"));
                }
                targets.push(Tsonic.CSharp.Node.path.resolve(siteDir, "layouts"));
                targets.push(Tsonic.CSharp.Node.path.resolve(siteDir, "static"));
                return targets;
            };
            startWatchLoop = (ServeRequest req, Action<string> onRebuild) =>
            {
                Tsonic.CSharp.Js.JSArray<string> targets = collectWatchTargets(req);
                Tsonic.CSharp.Js.Map<string, WatchEntryState> snapshot = Node_modules_Tsumo_engine_src_watchSnapshot.createWatchSnapshot(targets);
                bool rebuilding = false;
                Tsonic.CSharp.Node.timers.setInterval(() =>
                {
                    if (rebuilding)
                    {
                        return;
                    }
                    Tsonic.CSharp.Js.Map<string, WatchEntryState> next = Node_modules_Tsumo_engine_src_watchSnapshot.createWatchSnapshot(targets);
                    if (Node_modules_Tsumo_engine_src_watchSnapshot.watchSnapshotsEqual(snapshot, next))
                    {
                        return;
                    }
                    snapshot = next;
                    rebuilding = true;
                    try
                    {
                        BuildResult result = Node_modules_Tsumo_engine_src_buildSite.buildSite(req);
                        onRebuild(result.outputDir);
                        logLine($"[tsumo] rebuilt → {result.outputDir}");
                    }
                    catch (System.Exception __tsonic_catch0)
                    {
                        Tsonic.CSharp.Runtime.TsValue error = Tsonic.CSharp.Runtime.TsThrownValueException.toValue(__tsonic_catch0);
                        string message = Tsonic.CSharp.Runtime.TsValue.IsDynamicInstanceOf<TsumoError>(error) ? Tsonic.CSharp.Runtime.TsValue.CastDynamic<TsumoError>(error).diagnostic.format() : $"{error}";
                        logErrorLine($"[tsumo] rebuild failed: {message}");
                    }
                    finally
                    {
                        rebuilding = false;
                    }
                }, (int)250);
            };
            serveSite = (ServeRequest req) =>
            {
                string host = Tsonic.CSharp.Js.String.trim(req.host) == "" ? "localhost" : Tsonic.CSharp.Js.String.trim(req.host);
                int port = req.port;
                string prefix = $"http://{host}:{port}/";
                string? baseURL = req.baseURL;
                if (baseURL is null || Tsonic.CSharp.Js.String.trim(baseURL) == "")
                {
                    req.baseURL = Node_modules_Tsumo_engine_src_utils_text.ensureTrailingSlash(prefix);
                }
                string outputDir = Node_modules_Tsumo_engine_src_buildSite.buildSite(req).outputDir;
                Tsonic.CSharp.Node.Http.Server server = Tsonic.CSharp.Node.Http.http.createServer((Tsonic.CSharp.Node.Http.IncomingMessage request, Tsonic.CSharp.Node.Http.ServerResponse response) =>
                {
                    handleRequest(outputDir, request, response);
                });
                server.listen(port, host, () =>
                {
                    logLine("");
                    logLine("=================================");
                    logLine("  tsumo server");
                    logLine($"  Serving: {outputDir}");
                    logLine($"  URL: {prefix}");
                    logLine("=================================");
                    logLine("");
                    logLine("Press Ctrl+C to stop");
                });
                if (req.watch)
                {
                    startWatchLoop(req, (string rebuiltOutputDir) =>
                    {
                        outputDir = rebuiltOutputDir;
                    });
                }
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
