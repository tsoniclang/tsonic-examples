using System;

namespace Tsumo.Cli
{
    public static class Node_modules_Tsumo_engine_src_frontmatter_json
    {
        public static TsumoError invalidShape(string field, string expected, JsonValue value, string? sourcePath = null)
        {
            return Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_FIELD_INVALID", $"Front matter field '{field}' requires {expected}", sourcePath, value.line, value.column);
        }
        public static Func<string, JsonValue, string?, string> requireString
        {
            get;
            private set;
        } = default(Func<string, JsonValue, string?, string>)!;
        public static Func<string, JsonValue, string?, int> requireInt
        {
            get;
            private set;
        } = default(Func<string, JsonValue, string?, int>)!;
        public static Func<string, JsonValue, string?, Tsonic.CSharp.Js.JSArray<string>> requireStringArray
        {
            get;
            private set;
        } = default(Func<string, JsonValue, string?, Tsonic.CSharp.Js.JSArray<string>>)!;
        public static Func<string, JsonValue, string?, ParamValue> toParam
        {
            get;
            private set;
        } = default(Func<string, JsonValue, string?, ParamValue>)!;
        public static Action<JsonObject, string, string?> assertCaseInsensitiveKeysUnique
        {
            get;
            private set;
        } = default(Action<JsonObject, string, string?>)!;
        public static Action<FrontMatterMenu, string, JsonValue, string?> applyMenuProperty
        {
            get;
            private set;
        } = default(Action<FrontMatterMenu, string, JsonValue, string?>)!;
        public static Func<string, string?, FrontMatter> parseJsonFrontMatter
        {
            get;
            private set;
        } = default(Func<string, string?, FrontMatter>)!;
        private static readonly System.Lazy<object?> __tsonic_module_initialization = new System.Lazy<object?>(() => __tsonic_module_init_core());
        private static object? __tsonic_module_init_core()
        {
            Node_modules_Tsumo_engine_src_diagnostics.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_params.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_int32.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_utils_json.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_frontmatter_data.__tsonic_module_init();
            Node_modules_Tsumo_engine_src_frontmatter_menu.__tsonic_module_init();
            requireString = (string field, JsonValue value, string? sourcePath) =>
            {
                if (value is JsonString)
                {
                    return ((JsonString)value).value;
                }
                throw invalidShape(field, "a string", value, sourcePath);
            };
            requireInt = (string field, JsonValue value, string? sourcePath) =>
            {
                if (value is JsonNumber)
                {
                    int? narrowed = Node_modules_Tsumo_engine_src_utils_int32.toInt32(((JsonNumber)value).value);
                    if (narrowed is not null)
                    {
                        return narrowed.Value;
                    }
                }
                throw invalidShape(field, "a 32-bit integer", value, sourcePath);
            };
            requireStringArray = (string field, JsonValue value, string? sourcePath) =>
            {
                if (!(value is JsonArray))
                {
                    throw invalidShape(field, "an array of strings", value, sourcePath);
                }
                JsonArray array = (JsonArray)value;
                Tsonic.CSharp.Js.JSArray<string> result = new Tsonic.CSharp.Js.JSArray<string>(new string[] { });
                for (int index = 0; index < array.items.length; index++)
                {
                    JsonValue item = array.items[index];
                    if (item is JsonString)
                    {
                        result.push(((JsonString)item).value);
                    }
                    else
                    {
                        throw invalidShape(field, "an array containing only strings", item, sourcePath);
                    }
                }
                return result;
            };
            toParam = (string field, JsonValue value, string? sourcePath) =>
            {
                if (value is JsonString)
                {
                    return ParamValue.@string(((JsonString)value).value);
                }
                if (value is JsonBool)
                {
                    return ParamValue.@bool(((JsonBool)value).value);
                }
                if (value is JsonNumber)
                {
                    return ParamValue.number(requireInt(field, (JsonNumber)value, sourcePath));
                }
                throw invalidShape(field, "a string, boolean, or 32-bit integer", value, sourcePath);
            };
            assertCaseInsensitiveKeysUnique = (JsonObject value, string context, string? sourcePath) =>
            {
                Tsonic.CSharp.Js.Set<string> keys = new Tsonic.CSharp.Js.Set<string>();
                for (int index = 0; index < value.properties.length; index++)
                {
                    JsonProperty property = value.properties[index];
                    string key = Tsonic.CSharp.Js.String.toLowerCase(property.key);
                    if (keys.has(key))
                    {
                        throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_FIELD_DUPLICATE", $"{context} field '{property.key}' is declared more than once", sourcePath, property.line, property.column);
                    }
                    keys.add(key);
                }
            };
            applyMenuProperty = (FrontMatterMenu entry, string keyRaw, JsonValue value, string? sourcePath) =>
            {
                string key = Tsonic.CSharp.Js.String.toLowerCase(keyRaw);
                if (key == "weight")
                {
                    entry.weight = requireInt(keyRaw, value, sourcePath);
                }
                else
                {
                    if (key == "name")
                    {
                        entry.name = requireString(keyRaw, value, sourcePath);
                    }
                    else
                    {
                        if (key == "parent")
                        {
                            entry.parent = requireString(keyRaw, value, sourcePath);
                        }
                        else
                        {
                            if (key == "identifier")
                            {
                                entry.identifier = requireString(keyRaw, value, sourcePath);
                            }
                            else
                            {
                                if (key == "pre")
                                {
                                    entry.pre = requireString(keyRaw, value, sourcePath);
                                }
                                else
                                {
                                    if (key == "post")
                                    {
                                        entry.post = requireString(keyRaw, value, sourcePath);
                                    }
                                    else
                                    {
                                        if (key == "title")
                                        {
                                            entry.title = requireString(keyRaw, value, sourcePath);
                                        }
                                        else
                                        {
                                            throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_MENU_FIELD_UNKNOWN", $"Unknown front matter menu field '{keyRaw}'", sourcePath, value.line, value.column);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            parseJsonFrontMatter = (string text, string? sourcePath) =>
            {
                JsonValue rootValue = Node_modules_Tsumo_engine_src_utils_json.parseJson(text, sourcePath);
                if (!(rootValue is JsonObject))
                {
                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_ROOT_INVALID", "JSON front matter requires an object", sourcePath, rootValue.line, rootValue.column);
                }
                JsonObject root = (JsonObject)rootValue;
                assertCaseInsensitiveKeysUnique(root, "Front matter", sourcePath);
                FrontMatter frontMatter = new FrontMatter();
                for (int index = 0; index < root.properties.length; index++)
                {
                    JsonProperty property = root.properties[index];
                    string key = Tsonic.CSharp.Js.String.toLowerCase(property.key);
                    JsonValue value = property.value;
                    if (key == "title")
                    {
                        frontMatter.title = requireString(property.key, value, sourcePath);
                    }
                    else
                    {
                        if (key == "description")
                        {
                            frontMatter.description = requireString(property.key, value, sourcePath);
                        }
                        else
                        {
                            if (key == "slug")
                            {
                                frontMatter.slug = requireString(property.key, value, sourcePath);
                            }
                            else
                            {
                                if (key == "layout")
                                {
                                    frontMatter.layout = requireString(property.key, value, sourcePath);
                                }
                                else
                                {
                                    if (key == "type")
                                    {
                                        frontMatter.type = requireString(property.key, value, sourcePath);
                                    }
                                    else
                                    {
                                        if (key == "draft")
                                        {
                                            if (value is JsonBool)
                                            {
                                                frontMatter.draft = ((JsonBool)value).value;
                                            }
                                            else
                                            {
                                                throw invalidShape(property.key, "a boolean", value, sourcePath);
                                            }
                                        }
                                        else
                                        {
                                            if (key == "date")
                                            {
                                                string authored = requireString(property.key, value, sourcePath);
                                                double milliseconds = Tsonic.CSharp.Js.Date.parse(authored);
                                                if (Tsonic.CSharp.Js.Number.isNaN(milliseconds))
                                                {
                                                    throw Node_modules_Tsumo_engine_src_diagnostics.createTsumoError("TSUMO_FRONTMATTER_INVALID_DATE", $"Invalid front matter date: {authored}", sourcePath, value.line, value.column);
                                                }
                                                frontMatter.date = new Tsonic.CSharp.Js.Date(milliseconds);
                                            }
                                            else
                                            {
                                                if (key == "tags")
                                                {
                                                    frontMatter.tags = requireStringArray(property.key, value, sourcePath);
                                                }
                                                else
                                                {
                                                    if (key == "categories")
                                                    {
                                                        frontMatter.categories = requireStringArray(property.key, value, sourcePath);
                                                    }
                                                    else
                                                    {
                                                        if (key == "params")
                                                        {
                                                            if (!(value is JsonObject))
                                                            {
                                                                throw invalidShape(property.key, "an object of scalar values", value, sourcePath);
                                                            }
                                                            JsonObject @params = (JsonObject)value;
                                                            assertCaseInsensitiveKeysUnique(@params, "Front matter params", sourcePath);
                                                            for (int paramIndex = 0; paramIndex < @params.properties.length; paramIndex++)
                                                            {
                                                                JsonProperty parameter = @params.properties[paramIndex];
                                                                frontMatter.Params.set(parameter.key, toParam(parameter.key, parameter.value, sourcePath));
                                                            }
                                                        }
                                                        else
                                                        {
                                                            if (key == "menu")
                                                            {
                                                                if (!(value is JsonObject))
                                                                {
                                                                    throw invalidShape(property.key, "an object", value, sourcePath);
                                                                }
                                                                JsonObject menuObject = (JsonObject)value;
                                                                assertCaseInsensitiveKeysUnique(menuObject, "Front matter menu", sourcePath);
                                                                for (int menuIndex = 0; menuIndex < menuObject.properties.length; menuIndex++)
                                                                {
                                                                    JsonProperty menu = menuObject.properties[menuIndex];
                                                                    if (!(menu.value is JsonObject))
                                                                    {
                                                                        throw invalidShape(menu.key, "a menu property object", menu.value, sourcePath);
                                                                    }
                                                                    JsonObject menuFields = (JsonObject)menu.value;
                                                                    assertCaseInsensitiveKeysUnique(menuFields, $"Front matter menu '{menu.key}'", sourcePath);
                                                                    FrontMatterMenu entry = new FrontMatterMenu(menu.key);
                                                                    for (int fieldIndex = 0; fieldIndex < menuFields.properties.length; fieldIndex++)
                                                                    {
                                                                        JsonProperty field = menuFields.properties[fieldIndex];
                                                                        applyMenuProperty(entry, field.key, field.value, sourcePath);
                                                                    }
                                                                    frontMatter.menus.push(entry);
                                                                }
                                                            }
                                                            else
                                                            {
                                                                frontMatter.Params.set(property.key, toParam(property.key, value, sourcePath));
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
                return frontMatter;
            };
            return null;
        }
        public static void __tsonic_module_init()
        {
            _ = __tsonic_module_initialization.Value;
        }
    }
}
