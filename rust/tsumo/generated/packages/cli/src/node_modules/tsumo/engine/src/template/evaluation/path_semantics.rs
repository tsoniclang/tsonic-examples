// Generated by the Tsonic Rust target. Do not edit.

use tsonic_rust_js::abi as js_abi;

use tsonic_rust_js::string as js_string;

use crate::program as rt;

pub fn normalize_rel_path(raw: String) -> rt::TsonicResult<String> {
    let normalized: String = crate::node_modules::tsumo::engine::src::utils::strings::replace_text(
        &raw,
        String::from("\\"),
        String::from("/"),
    )?;
    let parts: js_abi::JsArray<String> =
        js_string::split_all(&normalized, "/").map_err(tsonic_rust_runtime::TsonicError::from)?;
    let out_parts: js_abi::JsArray<String> = js_abi::JsArray::from_dense(vec![]);
    {
        let mut i: f64 = 0.0;
        'loop_value: while i < (tsonic_rust_runtime::conversions::usize_to_i32(parts.len())? as f64) {
            let p: String = js_string::trim(&match parts.get_number(i).as_ref() {
                Some(flow_value) => flow_value.clone(),
                None => unreachable!("checked flow selected a missing optional value"),
            });
            if p.is_empty() || p == "." {
                i += 1.0;
                continue 'loop_value;
            }
            if p == ".." {
                if tsonic_rust_runtime::conversions::usize_to_i32(out_parts.len())? > 0 {
                    out_parts.pop();
                }
                i += 1.0;
                continue 'loop_value;
            }
            tsonic_rust_runtime::conversions::usize_to_i32(out_parts.push_many([p.clone()]))?;
            i += 1.0;
        }
    }
    let arr: js_abi::JsArray<String> = out_parts.clone();
    let mut out: String = String::from("");
    {
        let mut i: f64 = 0.0;
        while i < (tsonic_rust_runtime::conversions::usize_to_i32(arr.len())? as f64) {
            out = if out.is_empty() { match arr.get_number(i).as_ref() {
    Some(flow_value_2) => flow_value_2.clone(),
    None => unreachable!("checked flow selected a missing optional value"),
} } else { format!("{}{}{}", out, String::from("/"), match arr.get_number(i).as_ref() {
    Some(flow_value_3) => flow_value_3.clone(),
    None => unreachable!("checked flow selected a missing optional value"),
}) };
            i += 1.0;
        }
    }
    Ok(out.clone())
}

pub fn segment_match(pattern: String, segment: String) -> rt::TsonicResult<bool> {
    if pattern == "*" {
        return Ok(true);
    }
    let star: i32 = tsonic_rust_runtime::conversions::isize_to_i32(js_string::index_of_from_start(
        &pattern, "*",
    ))?;
    if star < 0 {
        return Ok(pattern == segment);
    }
    let parts: js_abi::JsArray<String> =
        js_string::split_all(&pattern, "*").map_err(tsonic_rust_runtime::TsonicError::from)?;
    let mut pos: f64 = 0.0;
    {
        let mut i: f64 = 0.0;
        'loop_value: while i < (tsonic_rust_runtime::conversions::usize_to_i32(parts.len())? as f64) {
            let p: String = match parts.get_number(i).as_ref() {
                Some(flow_value) => flow_value.clone(),
                None => unreachable!("checked flow selected a missing optional value"),
            };
            if p.is_empty() {
                i += 1.0;
                continue 'loop_value;
            }
            let idx: i32 = tsonic_rust_runtime::conversions::isize_to_i32(js_string::index_of(
                &segment, &p, pos,
            ))?;
            if idx < 0 {
                return Ok(false);
            }
            if i == 0.0 && !js_string::starts_with_from_start(&pattern, "*") && idx != 0 {
                return Ok(false);
            }
            pos = tsonic_rust_runtime::conversions::i32_to_f64(
                idx + tsonic_rust_runtime::conversions::usize_to_i32(js_string::js_len(&p))?,
            );
            i += 1.0;
        }
    }
    if !js_string::ends_with_at_end(&pattern, "*")
        && pos
            != tsonic_rust_runtime::conversions::usize_to_i32(js_string::js_len(&segment))? as f64
    {
        return Ok(false);
    }
    Ok(true)
}

pub fn split_glob_segments(raw: &str) -> rt::TsonicResult<js_abi::JsArray<String>> {
    let slash: String = String::from("/");
    let normalized: String =
        crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_start_character(
            crate::node_modules::tsumo::engine::src::utils::strings::replace_text(
                &js_string::trim(raw),
                String::from("\\"),
                String::from("/"),
            )?,
            slash.clone(),
        )?;
    if normalized.is_empty() {
        let empty: js_abi::JsArray<String> = js_abi::JsArray::from_dense(vec![]);
        return Ok(empty.clone());
    }
    Ok(js_string::split_all(&normalized, "/").map_err(tsonic_rust_runtime::TsonicError::from)?)
}

pub fn glob_match_at(
    pat_segs: js_abi::JsArray<String>,
    path_segs: js_abi::JsArray<String>,
    pi: i32,
    si: i32,
) -> rt::TsonicResult<bool> {
    if pi >= tsonic_rust_runtime::conversions::usize_to_i32(pat_segs.len())? {
        return Ok(si >= tsonic_rust_runtime::conversions::usize_to_i32(path_segs.len())?);
    }
    let p: String = match pat_segs
        .get_number(tsonic_rust_runtime::conversions::i32_to_f64(pi))
        .as_ref()
    {
        Some(flow_value) => flow_value.clone(),
        None => unreachable!("checked flow selected a missing optional value"),
    };
    if p == "**" {
        {
            let mut i: i32 = si;
            while i <= tsonic_rust_runtime::conversions::usize_to_i32(path_segs.len())? {
                if glob_match_at(pat_segs.clone(), path_segs.clone(), pi + 1, i)? {
                    return Ok(true);
                }
                i += 1;
            }
        }
        return Ok(false);
    }
    if si >= tsonic_rust_runtime::conversions::usize_to_i32(path_segs.len())? {
        return Ok(false);
    }
    if !segment_match(p.clone(), match path_segs
        .get_number(tsonic_rust_runtime::conversions::i32_to_f64(si))
        .as_ref()
    {
        Some(flow_value_2) => flow_value_2.clone(),
        None => unreachable!("checked flow selected a missing optional value"),
    })?
    {
        return Ok(false);
    }
    glob_match_at(pat_segs.clone(), path_segs.clone(), pi + 1, si + 1)
}

pub fn glob_match(pattern_raw: String, path_raw: String) -> rt::TsonicResult<bool> {
    let pat_segs: js_abi::JsArray<String> = split_glob_segments(&pattern_raw)?;
    let path_segs: js_abi::JsArray<String> = split_glob_segments(&path_raw)?;
    glob_match_at(pat_segs.clone(), path_segs.clone(), 0, 0)
}

pub fn resolve_page_ref(
    page: crate::node_modules::tsumo::engine::src::models::page_context::PageContext,
    r#ref: &str,
) -> rt::TsonicResult<String> {
    let raw: String = js_string::trim(r#ref);
    if raw.is_empty() || raw == "/" {
        return Ok(String::from(""));
    }
    if js_string::starts_with_from_start(&raw, "/") {
        return crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_slashes(
            raw.clone(),
        );
    }
    let page_file: Option<crate::node_modules::tsumo::engine::src::models::page_file::PageFile> =
        page.state.with(|state| state.file.clone());
    let base: String = if page_file.is_some() {
        match page_file.as_ref() {
            Some(flow_value) => flow_value.clone(),
            None => unreachable!("checked flow selected a missing optional value"),
        }
        .state
        .with(|state| state.dir.clone())
    } else {
        crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_slashes(
            page.state.with(|state| state.rel_permalink.clone()),
        )?
    };
    let combined: String = if base.is_empty() {
        raw.clone()
    } else {
        format!(
            "{}{}{}",
            crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_end_character(
                base.clone(),
                String::from("/"),
            )?,
            String::from("/"),
            crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_start_character(
                raw.clone(),
                String::from("/"),
            )?,
        )
    };
    normalize_rel_path(combined.clone())
}

pub fn try_get_page(
    site: crate::node_modules::tsumo::engine::src::models::site_context::SiteContext,
    path_raw: &str,
) -> rt::TsonicResult<
    Option<crate::node_modules::tsumo::engine::src::models::page_context::PageContext>,
> {
    let trimmed: String = js_string::trim(path_raw);
    if trimmed.is_empty() || trimmed == "/" {
        return Ok(site.state.with(|state| state.home.clone()));
    }
    let needle: String =
        crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_slashes(
            trimmed.clone(),
        )?;
    if needle.is_empty() {
        return Ok(site.state.with(|state| state.home.clone()));
    }
    let mut candidates: js_abi::JsArray<
        crate::node_modules::tsumo::engine::src::models::page_context::PageContext,
    > = site.state.with(|state| state.pages.clone());
    if tsonic_rust_runtime::conversions::usize_to_i32(
        site.state.with(|state| state.all_pages.clone()).len(),
    )? > 0
    {
        candidates = site.state.with(|state| state.all_pages.clone());
    }
    {
        let mut i: f64 = 0.0;
        while i < (tsonic_rust_runtime::conversions::usize_to_i32(candidates.len())? as f64) {
            let p: crate::node_modules::tsumo::engine::src::models::page_context::PageContext =
                match candidates.get_number(i).as_ref() {
                    Some(flow_value) => flow_value.clone(),
                    None => unreachable!("checked flow selected a missing optional value"),
                };
            if crate::node_modules::tsumo::engine::src::template::evaluation::serialization::trim_slashes(
                p.state.with(|state| state.rel_permalink.clone()),
            )?
                == needle
            {
                return Ok(Some(p.clone()));
            }
            if p.state.with(|state| state.slug.clone()) == needle {
                return Ok(Some(p.clone()));
            }
            i += 1.0;
        }
    }
    Ok(Option::<crate::node_modules::tsumo::engine::src::models::page_context::PageContext>::None)
}
