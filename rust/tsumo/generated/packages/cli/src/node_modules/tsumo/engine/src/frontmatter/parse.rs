// Generated by the Tsonic Rust target. Do not edit.

use tsonic_rust_js::abi as js_abi;

use tsonic_rust_js::string as js_string;

use crate::program as rt;

pub(crate) fn try_parse_json_front_matter(
    text: String,
    source_path: Option<String>,
) -> rt::TsonicResult<
    Option<crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent>,
> {
    let start: i32 = 0;
    if tsonic_rust_runtime::conversions::usize_to_i32(js_string::js_len(&text))? == 0
        || js_string::char_at(&text, tsonic_rust_runtime::conversions::i32_to_f64(start))
            .map_err(tsonic_rust_runtime::TsonicError::from)?
            != "{"
    {
        return Ok(
            Option::<crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent>::None,
        );
    }
    let mut depth: i32 = 0;
    let mut in_string: bool = false;
    let mut escaped: bool = false;
    let mut end: i32 = -1;
    {
        let mut index: i32 = start;
        'loop_value: while index < tsonic_rust_runtime::conversions::usize_to_i32(js_string::js_len(&text))? {
            let current: String = js_string::char_at(
                &text,
                tsonic_rust_runtime::conversions::i32_to_f64(index),
            )
            .map_err(tsonic_rust_runtime::TsonicError::from)?;
            if in_string && escaped {
                escaped = false;
                index += 1;
                continue 'loop_value;
            }
            if in_string && current == "\\" {
                escaped = true;
                index += 1;
                continue 'loop_value;
            }
            if current == "\"" {
                in_string = !in_string;
                index += 1;
                continue 'loop_value;
            }
            if in_string {
                index += 1;
                continue 'loop_value;
            }
            if current == "{" {
                depth += 1;
            } else {
                if current == "}" {
                    depth -= 1;
                    if depth == 0 {
                        end = index + 1;
                        break 'loop_value;
                    }
                }
            }
            index += 1;
        }
    }
    if end < 0 {
        return Err(rt::TsonicError::from(crate::node_modules::tsumo::engine::src::diagnostics::create_tsumo_error(
            String::from("TSUMO_FRONTMATTER_JSON_UNCLOSED"),
            String::from("JSON front matter has no closing object delimiter"),
            source_path.clone(),
            Some(1.0),
            Some(1.0),
        )));
    }
    let input: String = crate::node_modules::tsumo::engine::src::utils::strings::substring_count(
        text.clone(),
        start,
        end - start,
    )?;
    let body: String = js_string::trim_start(
        &crate::node_modules::tsumo::engine::src::utils::strings::substring_from(&text, end)?,
    );
    Ok(
        Some(crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent::new(
            crate::node_modules::tsumo::engine::src::frontmatter::json::parse_json_front_matter(
                input.clone(),
                source_path.clone(),
            )?,
            body.clone(),
        )),
    )
}

pub(crate) fn parse_delimited_front_matter(
    lines: js_abi::JsArray<String>,
    delimiter: String,
    format: String,
    source_path: Option<String>,
) -> rt::TsonicResult<
    crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent,
> {
    let front_matter_lines: js_abi::JsArray<String> = js_abi::JsArray::from_dense(vec![]);
    #[expect(unused_assignments, reason = "checked source evaluation order")]
    let mut body_start: i32 = tsonic_rust_runtime::conversions::usize_to_i32(lines.len())?;
    {
        let mut index: i32 = 1;
        while index < tsonic_rust_runtime::conversions::usize_to_i32(lines.len())? {
            if js_string::trim(&match lines
                .get_number(tsonic_rust_runtime::conversions::i32_to_f64(index))
                .as_ref()
            {
                Some(flow_value) => flow_value.clone(),
                None => unreachable!("checked flow selected a missing optional value"),
            }) == delimiter
            {
                body_start = index + 1;
                let body: String = js_string::trim_start(&lines
                    .slice_from(tsonic_rust_runtime::conversions::i32_to_f64(body_start))
                    .join("\n"));
                let front_matter: crate::node_modules::tsumo::engine::src::frontmatter::data::FrontMatter =
                    if format == "yaml" {
                        crate::node_modules::tsumo::engine::src::frontmatter::yaml::parse_yaml_front_matter(
                            front_matter_lines.clone(),
                            source_path.clone(),
                        )?
                    } else {
                        crate::node_modules::tsumo::engine::src::frontmatter::toml::parse_toml_front_matter(
                            front_matter_lines.clone(),
                            source_path.clone(),
                        )?
                    };
                return Ok(crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent::new(
                    front_matter.clone(),
                    body.clone(),
                ));
            }
            tsonic_rust_runtime::conversions::usize_to_i32(
                front_matter_lines.push_many([match lines
                    .get_number(tsonic_rust_runtime::conversions::i32_to_f64(index))
                    .as_ref()
                {
                    Some(flow_value_2) => flow_value_2.clone(),
                    None => unreachable!("checked flow selected a missing optional value"),
                }]),
            )?;
            index += 1;
        }
    }
    Err(rt::TsonicError::from(crate::node_modules::tsumo::engine::src::diagnostics::create_tsumo_error(
        String::from("TSUMO_FRONTMATTER_DELIMITER_UNCLOSED"),
        format!(
            "{}{}{}{}{}",
            String::from(""),
            rt::source_string(&if format == "yaml" {
                String::from("YAML")
            } else {
                String::from("TOML")
            },),
            String::from(" front matter is missing its closing "),
            rt::source_string(&delimiter),
            String::from(" delimiter"),
        ),
        source_path.clone(),
        Some(1.0),
        Some(1.0),
    )))
}

pub fn parse_content(
    text: String,
    source_path: Option<String>,
) -> rt::TsonicResult<
    crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent,
> {
    let json: Option<
        crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent,
    > = try_parse_json_front_matter(text.clone(), source_path.clone())?;
    if json.is_some() {
        return Ok(match json.as_ref() {
            Some(flow_value) => flow_value.clone(),
            None => unreachable!("checked flow selected a missing optional value"),
        });
    }
    let normalized: String =
        crate::node_modules::tsumo::engine::src::utils::strings::replace_line_endings(
            &text,
            String::from("\n"),
        )?;
    let lines: js_abi::JsArray<String> = js_string::split_all(&normalized, "\n")
        .map_err(tsonic_rust_runtime::TsonicError::from)?;
    if tsonic_rust_runtime::conversions::usize_to_i32(lines.len())? == 0 {
        return Ok(crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent::new(
            crate::node_modules::tsumo::engine::src::frontmatter::data::FrontMatter::new(),
            String::from(""),
        ));
    }
    let first_line: String = js_string::trim(&match lines.get_number(0.0).as_ref() {
        Some(flow_value_2) => flow_value_2.clone(),
        None => unreachable!("checked flow selected a missing optional value"),
    });
    if first_line == "---" {
        return parse_delimited_front_matter(
            lines.clone(),
            String::from("---"),
            String::from("yaml"),
            source_path.clone(),
        );
    }
    if first_line == "+++" {
        return parse_delimited_front_matter(
            lines.clone(),
            String::from("+++"),
            String::from("toml"),
            source_path.clone(),
        );
    }
    Ok(crate::node_modules::tsumo::engine::src::frontmatter::parsed_content::ParsedContent::new(
        crate::node_modules::tsumo::engine::src::frontmatter::data::FrontMatter::new(),
        text.clone(),
    ))
}
